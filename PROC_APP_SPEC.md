# KCSC Proc — Project Specification
## Built on Frappe Framework / ERPNext v16

---

> **Document Status:** `DRAFT v0.1`
> **Last Updated:** 2026-08-03
> **Maintained by:** KCSC — Karama Computer Services Company
> **App Name:** `proc_app`
> **Target Version:** ERPNext v16.30 / Frappe v16.29
> **Deployment Model:** Multi-client reusable app — standalone-capable, no dependency on any supplier portal. `supplier_portal` (VendorGate) optionally depends on this app; this app never depends on `supplier_portal`.
> **Update Policy:** This document MUST be updated whenever a feature, flow, architecture decision, or implementation detail changes. Every Claude Code session should begin by reading this file and end by proposing updates to it.

---

## 1. Project Overview

### 1.1 Purpose

KCSC Proc is the reusable back-office procurement system underlying KCSC's procurement solution suite. It covers Procurement Management (Purchase Requisition through Goods Receipt), Stock Management, and procurement/stock reporting, built entirely on ERPNext v16 out-of-the-box functionality wherever possible, with custom development limited to genuine gaps only.

KCSC Proc is designed to function as a complete, standalone procurement system via the ERPNext desk alone — a client may deploy it with or without a supplier-facing portal.

### 1.2 Relationship to supplier_portal (VendorGate)

`supplier_portal` is a separate, optional companion app that provides supplier self-service UI on top of KCSC Proc. The dependency is one-directional: `supplier_portal` depends on `proc_app`; `proc_app` never depends on `supplier_portal`. Full rationale and the ownership decision log for what belongs in which app is documented in `PROC_SUITE_ARCHITECTURE.md` (this app's repo), Section 1 (App Boundary & Ownership) — this content was originally written into `SUPPLIER_PORTAL_SPEC.md` Section 16 during Phase 2 of the supplier_portal project, then relocated here on 2026-08-06.

### 1.3 Status

This app was scaffolded on 2026-08-03 (re-scaffolded from the earlier `karama_proc` under a new name, before any real content existed — see Section 4 below). It currently contains no custom doctypes, fields, or business logic — installation shell only.

### 1.4 Scope (Planned — Not Yet Built)

Per `PROC_SUITE_ARCHITECTURE.md` Sections 2 and 3 (OOB-vs-Custom Decision Log, Notifications Master List — this app's repo, relocated from `SUPPLIER_PORTAL_SPEC.md` on 2026-08-06, see Section 4 below), pending items include:
- Purchase Order Amendment (the one confirmed genuine custom-build gap)
- Migration of `Supplier ASN`, `Supplier ASN Item`, `Supplier Invoice Dispute` doctypes from `supplier_portal`
- Migration of PO acknowledgment fields and Supplier onboarding fields from `supplier_portal`
- Purchase Requisition + approval workflow (OOB via Material Request + Frappe Workflow)
- Stock Management (OOB — Item, Warehouse, Stock Entry, Reorder Tool, Stock Reconciliation)
- Procurement and Stock reports (mix of OOB standard reports + custom Query Reports)
- Roles & Permissions (draft baseline adopted — see `PROC_SUITE_ARCHITECTURE.md` Section 1.6)

The relocation of cross-app architecture content (App Boundary, OOB-vs-Custom log, Notifications Master List, Roles & Permissions) from `SUPPLIER_PORTAL_SPEC.md` into `PROC_SUITE_ARCHITECTURE.md` in this app's repo is complete as of 2026-08-06 — see Section 4 below for the record of that move. All items listed above remain genuinely not started; only the relocation of the planning content itself is done.

---

## 2. Development Phases

| Phase | Status | Description |
|---|---|---|
| 0 — Scaffolding | ✅ Complete (2026-08-03) | App created (as `karama_proc`, then renamed to `proc_app` before any real content existed), installed on site1.local, this spec file initialized |
| 1 — Doctype Migration | ✅ Complete (2026-08-14) | All 3 stages done: doctypes, custom fields, and API/hooks business logic all moved/split. proc_app is now genuinely standalone-capable for this scope. |
| 2 — Purchase Requisition & Approval Workflow | ✅ Built, tested, and proven end-to-end, including the Forward-to-Purchase spawn redesign (2026-08-09). See Section 3. | Configure Material Request + Frappe Workflow |
| 3 — Stock Management | ✅ OOB-verified (2026-08-14) | Item, Warehouse, Stock Entry, Stock Reconciliation, and all 3 native reports (Stock Balance, Stock Ledger, Stock Ageing) confirmed genuinely working via live testing — no custom code needed. Reorder Tool confirmed importable/callable but its actual triggering behavior (below-threshold → auto Material Request) remains untested, since no items currently have reorder_level configured — an honest data gap, not a bug. |
| 4 — PO Amendment (custom build) | ✅ Built and tested (2026-08-14) | The one confirmed custom-doctype gap. `Purchase Order Amendment` doctype + approval workflow built and tested end-to-end (Draft → Submit → Approve). Separate internal record — does not replace supplier_portal's existing comment-based amendment request. ⚠️ Documentation-only: Approval does not automatically update the actual Purchase Order's fields, nor notify the supplier. Deliberately deferred (2026-08-22, confirmed with Yasser during hands-on testing) — a procurement staff member must still manually apply the approved change to the real PO. Revisit once real business requirements are confirmed (post-workshop). |
| 5 — Reports | 🟡 Started (2026-08-23) | Audit of all 7 Procurement Reports against ERPNext's native reporting library complete: 3 fully OOB-reusable (`Supplier Quotation Comparison` exact match; `Accounts Payable` for Invoice Aging, confirmed via real filter definitions — `ageing_based_on`/`range`; `Purchase Order Analysis`/`Purchase Order Trends` for PO Status Tracker), 3 partially covered by native reports with real gaps noted (`Procurement Tracker` for Requisition Status — tracks `po.status`, not our custom `workflow_state`; `Purchase Order Analysis` for PO vs GRN Variance — has Received/Pending Qty but isn't purpose-built for variance; `Purchase Register`/`Item-wise Purchase Register` for Spending by Supplier/Category — usable via the report viewer's own Group By, not dedicated reports), 1 genuine gap now built (`Purchase Requisition Status`, see Changelog). Stock Reports audit also complete: `Stock Balance` (already OOB-verified) directly covers Stock Valuation; `Items Below Reorder Level` was a genuine gap (the native Reorder Tool is a silent background job only — `Itemwise Recommended Reorder Level` calculates a suggested level rather than comparing against the real one, `Item Shortage Report` uses a different negative-projected-qty threshold) — now built; `Physical Count Variance` also now built (cross-document view of `Stock Reconciliation` variances — real per-document data existed already, no report showed it across all reconciliations at once). Stock Movement Summary remains a confirmed gap, not yet built. |
| 6 — Roles & Permissions | ⬜ Not started | Fixtures-based role/permission setup, following supplier_portal's pattern |

---

## 3. Material Request Approval Workflow — Design (Not Yet Built)

### 3.1 Purpose

This documents the design for proc_app's first real feature: a Material Request approval workflow, built entirely on ERPNext v16 OOB capabilities (Material Request doctype + Frappe Workflow engine) plus 4 custom fields. Designed 2026-08-07, based on a real example workflow provided by KCSC, before the client workshop — treated as a standard baseline per the same "adopt now, refine later" principle as Section 1.6 of PROC_SUITE_ARCHITECTURE.md.

### 3.2 Reference Example (source of this design)

A Finance Department employee needs printer ink. They create a Material Request addressed to IT (the department that manages that item type). The Finance Department Manager approves first. Then IT's "person in charge" (Officer) checks stock: if available, it's issued directly from IT's warehouse to Finance; if not, it goes to IT's Manager for approval, then to Procurement's Officer for final approval, before becoming a Purchase Order.

### 3.3 Custom Fields Required

On Material Request:
- requesting_department (Link to Department) — the department that needs the item, e.g. Finance
- concerned_department (Link to Department) — the department that manages/reviews that item type, e.g. IT

On Department:
- department_manager (Link to User) — this department's manager, approves requests where this department is either the requesting or concerned department
- department_officer (Link to User) — this department's person in charge, reviews stock availability and decides the issue-vs-purchase branch when this department is the concerned department

### 3.4 New Roles Required

- Department Manager — generic role, held by whoever manages any department. Actual approval authority for a specific request is enforced via a Workflow transition condition checking that the acting user matches the relevant department's department_manager field, not by the role alone.
- Department Officer — same pattern, checked against department_officer.
- Procurement's final approval reuses the existing draft Procurement Officer role (see PROC_SUITE_ARCHITECTURE.md, Section 1.6) — no new role needed there.

### 3.5 Workflow States & Transitions

Draft, then Submit moves it to Pending Requesting-Department Approval, where the approver is the requesting department's department_manager. Approve moves it to Pending Concerned-Department Review, where the approver is the concerned department's department_officer. From there, two branches: Approve as Issue from Stock moves it directly to Approved – Issue (docstatus: Submitted). Approve as Forward to Purchase moves it to Pending Concerned-Department Manager Approval, where the approver is the concerned department's department_manager; Approve there moves it to Pending Procurement Approval, where the approver holds the Procurement Officer role; Approve there moves it to Approved – Purchase (docstatus: Submitted). A Reject action is available at every approval step and moves the request to Rejected.

Each department-scoped approval uses a Workflow Transition condition (confirmed available on Frappe's Workflow Transition doctype, condition field, Code type) — e.g., a transition is only enabled for the acting user if they match the relevant department's department_manager or department_officer field for the specific department linked on that request. This means the same 2 roles work correctly for every department, without per-department configuration beyond setting the 2 Department fields.

Approved – Issue and Approved – Purchase are both docstatus: Submitted states — at that point, ERPNext's existing native Create actions (confirmed present, no custom code needed) become available: make_stock_entry() for the issue path, make_purchase_order() / make_request_for_quotation() for the purchase path.

### 3.6 Deliberately Deferred (Not Built in This Phase)

- Free-text / not-yet-in-system items: ERPNext's Material Request Item.item_code is mandatory at the Frappe framework level (confirmed via code audit, not just a UI restriction) — allowing a supplier or requester to describe an item that doesn't exist yet in the Item master would require genuine custom code (a flag + description field, plus a script to auto-create the Item record at PO time). This is a real, scoped piece of custom work, deliberately deferred until the core approval workflow is built and proven. Not forgotten — tracked here.
- Approval value thresholds: still pending the client workshop (see PROC_SUITE_ARCHITECTURE.md, Section 1.6) — this workflow's approvals are role/department-based, not value-based, for now.
- **Self-approval:** `allow_self_approval` was left at Frappe's default (`1`, allowed) on all Workflow transitions. This was a deliberate decision (2026-08-08), not an oversight — confirmed explicitly rather than assumed. If a single person holds both the requester's identity and the relevant approval role for a given request, they are permitted to approve their own request at any stage.
- **Submit-step department scoping:** The initial Draft → Submit transition only checks that the acting user holds the "Department User" role — it does not verify they actually belong to the specific `requesting_department` they're filing the request on behalf of. Enforcing that would require an Employee-to-Department assignment concept that doesn't exist yet in this design. Deliberately deferred, consistent with "start standard, refine later" — anyone with the Department User role can currently file a request on behalf of any department.
- **Permission hook timing differs from supplier_portal's pattern:** `proc_app`'s DocPerm setup (`setup.py`) is registered via `after_migrate`, not `after_install` like `supplier_portal`'s equivalent. This was a deliberate choice (2026-08-08): `after_install` only fires once, at initial app installation, which made it untestable on an already-installed app during development. `after_migrate` re-asserts permissions on every migrate, which is arguably more robust long-term (self-healing if permissions ever drift) at the cost of running slightly more often. Idempotency confirmed via delete-then-recreate before each grant — safe to run repeatedly.
- **Workflow activation is not fixture-managed:** `is_active` on the "Material Request Approval" Workflow document stays committed as `0` in `fixtures/workflow.json` permanently — this is the correct, safe default for a fresh client install (a workflow should never auto-activate before an admin has configured departments, managers, and officers). Discovered 2026-08-08: `bench migrate` re-syncs the Workflow from its fixture on every run, silently reverting a runtime activation back to `0`. **Correct activation procedure** (also discovered 2026-08-08, the hard way): do NOT toggle `is_active` via `frappe.db.set_value()` — this updates the database but does not invalidate Frappe's `workflow` cache key (`frappe.cache.hget("workflow", <doctype>)`), which is checked with a `is None` test, not a falsy check — so a stale cached empty string persists indefinitely even after the DB is corrected, silently disabling workflow_state defaulting and all transition validation. The correct way to activate/deactivate is via the standard Document API (`doc = frappe.get_doc("Workflow", ...); doc.is_active = 1; doc.save()`), which triggers normal cache invalidation, OR by explicitly clearing the cache key afterward (`frappe.cache.hdel("workflow", "Material Request")`) if a direct DB update is used. **Operational consequence:** activating the workflow for real use (or for testing) must be done as a deliberate action after every `migrate`, using `doc.save()` — not raw SQL/ORM value-setting — and must not persist to the fixture.
- **Supporting doctype read permissions found incomplete via manual browser testing:** console-based testing (`doc.insert()` under a test user) only exercises document-level and field-level permission checks — it does not exercise the desk UI's Link field dropdowns, which independently check read permission on the linked doctype when a user opens a Link field to search/select a value. Yasser's manual browser test surfaced two more doctypes needing read access that console testing had missed: `Company` and `Department`. Added to `SUPPORTING_DOCTYPES` in `setup.py` (2026-08-09), bringing the read-only supporting-doctype list to `Item`, `Item Group`, `UOM`, `Warehouse`, `Brand`, `Company`, `Department` — all 4 workflow roles, read-only. Applied directly via `setup_supporting_doctype_permissions()` (not a full `migrate`, to avoid re-triggering the Workflow `is_active` revert documented above). Worth remembering for future permission gaps: UI-driven testing and console-driven testing surface different failure classes — neither alone is sufficient.
- **Department Officer requires ERPNext's native "Stock User" role for the Issue-from-Stock branch:** Discovered via manual browser testing (2026-08-09) — creating a Stock Entry from a Material Request in "Approved - Issue" state requires permissions on the Stock Entry doctype itself, which our custom permission matrix (Section 3, setup.py) never granted (we only gave read-only access to Warehouse as a supporting doctype, not write/create/submit on Stock Entry). Rather than expand Department Officer's own Custom DocPerm to cover this, the correct fix is assigning ERPNext's existing native "Stock User" role (a standard OOB role, not something proc_app manages) to whichever user account is acting as Department Officer for a given department — keeping Department Officer itself narrowly scoped to department-approval concerns only, and stock-transaction rights separate. **Operational consequence:** any real person assigned as a department's `department_officer` must also hold ERPNext's standard "Stock User" role (or equivalent) to actually execute the Issue-from-Stock action — this is a per-deployment user-assignment step, not something proc_app's fixtures configure automatically, consistent with the standing "roles in code, user assignment in the desk" principle.

### 3.7 Live Test Results (2026-08-08) — PASSED

The full purchase-branch path was tested end-to-end against real Frappe permission/workflow enforcement (using `apply_workflow()`, not manual field manipulation), with 5 test users and 2 real seeded departments (Accounts - K as requesting, Operations - K as concerned).

**Confirmed working:**
- Draft → Submit (by Department User)
- Requesting-Dept Approval, correctly scoped to the specific department's manager — a wrong-department Department Manager was tested and correctly blocked (`WorkflowTransitionError`), document state unaffected
- Concerned-Dept Review — a wrong-role attempt (Department Manager instead of Department Officer) was tested and correctly blocked at the role gate itself, before any condition was evaluated
- Approve - Forward to Purchase branch, correctly routed to Concerned-Dept Manager Approval
- Concerned-Dept Manager Approval → Procurement Approval → **Approved - Purchase**, with `docstatus` correctly transitioning 0 → 1 (genuinely submitted, confirmed via direct DB query, not just workflow_state label)

**Not yet tested:** the Issue-from-Stock branch (Approve - Issue from Stock → Approved - Issue), and the Reject path at each stage. Both use the same proven mechanisms (role gate + department condition) as the tested purchase branch, so are expected to work, but should be verified explicitly before considering the workflow fully proven in all paths.

**Environment state after testing:** test document deleted (cancelled then force-deleted) to leave a clean slate. Workflow left `is_active: 1` at the database level per request — note this is NOT reflected in the committed fixture (still `0`, correctly, per Section 3.6) and will silently revert to inactive on the next `bench migrate`, consistent with the documented activation procedure.

### 3.8 Redesign — Forward to Purchase Now Spawns a Linked Document (2026-08-09)

**Problem found via manual browser testing:** ERPNext's native "Create" button on Material Request is gated entirely by `material_request_type`, not `workflow_state`. The original design (one document, type "Purchase" throughout, branching internally between issue/purchase) meant the Issue-from-Stock branch never had a working native path to create a Stock Entry — ERPNext only wires `make_stock_entry()` to the Material Transfer, Material Issue, and Customer Provided types, never Purchase.

**Redesign:** Requests are now created as `material_request_type = "Material Issue"` from the start.
- **Issue-from-Stock branch:** unchanged in structure — same document reaches `Approved - Issue`. Since the type is now Material Issue, ERPNext's native "Issue Material" Create button works correctly with zero custom UI needed.
- **Forward-to-Purchase branch:** the original document now transitions to a new terminal state, `Forwarded to Purchase` (docstatus: 1), and is also stopped via ERPNext's native `update_status("Stopped")` mechanism (confirmed reliable via code audit — correctly survives `status_map` recomputation, not a raw field write). A new custom field, `source_material_request` (Link → Material Request, read-only), and a new hook (`proc_app/material_request_hooks.py`, registered via `doc_events.Material Request.on_update`) automatically spawns a **new, separate** Material Request with `material_request_type = "Purchase"`, copying items, linking back via `source_material_request`, and pre-positioned directly at `Pending Concerned-Dept Manager Approval` — skipping the Requesting-Dept Approval and Concerned-Dept Review steps that already happened on the original. This new document then completes its own approval chain (Concerned-Dept Manager → Procurement) to reach `Approved - Purchase`, at which point ERPNext's native Purchase Order/RFQ Create buttons work correctly, since its type is genuinely "Purchase."

**Why this approach over a custom button:** avoids fighting ERPNext's native gating logic entirely — both resulting documents have a `material_request_type` that honestly matches their actual native behavior, so no custom UI/buttons are needed anywhere in the flow.

**Hook idempotency:** guarded against re-triggering on every subsequent save (checks `get_doc_before_save()`) and against duplicate spawning (checks for an existing linked document before creating another).

**Tested 2026-08-09, end-to-end, zero exceptions:** both branches proven — Issue-from-Stock (manual browser test) and Forward-to-Purchase spawn-and-complete (console test, full chain from Draft through both documents reaching their respective terminal states).

---

## 4. Known Issues & Decisions Log

| Date | Type | Description | Decision / Resolution |
|---|---|---|---|
| 2026-08-27 | Resolved — Proven Correct | **Closes the entry directly below (same day)**: the multi-supplier PO grouping logic, previously only exercised against a trivial 1-item RFQ, is now genuinely proven with real, adversarial data | Built a fresh 2-item RFQ (`SKU010`, `SKU009`) with deliberately reversed price advantage — `MA Inc.` cheaper on `SKU010`, `Summit Traders Ltd.` cheaper on `SKU009` — specifically so a naive "one supplier wins everything" bug couldn't hide behind a coincidentally-consistent winner. Comparison sheet scoring confirmed the genuine split (`MA Inc.` rank 1 on `SKU010`, `Summit Traders Ltd.` rank 1 on `SKU009`) *before* generating POs, per this task's own discipline of not assuming. `generate_purchase_orders_from_comparison()` produced exactly 2 POs, each containing only that supplier's own winning item — independently re-verified via a fresh SQL join across both POs' item rows, not the console's own print. Full detail in Changelog v1.30. |
| 2026-08-27 | Found & Fixed (recurring operational gap, newly understood scope) | Building the multi-supplier grouping proof above hit `apply_workflow(rfq, "Submit")` failing with `DoesNotExistError: Workflow  not found` — all 3 project workflows were genuinely `is_active=0` in the live DB, confirmed via fresh SQL, blocking any real workflow-based document creation | Root-caused, not just worked around: the prior task's (v1.29) closing `bench migrate` re-imported `workflow.json` via `sync_fixtures()` — and since the v1.17 pre-commit hook always resets the *committed fixture* to `is_active=0` before every commit (working exactly as designed, keeping git diffs clean), that correctly-zeroed fixture gets faithfully re-applied to the *live DB* on the very next migrate, deactivating the real workflows. **This is a previously undocumented consequence of v1.17's actual scope, not a new bug and not a failure of that fix**: the hook was built to solve a git-hygiene problem (dirty diffs from `export-fixtures` capturing live state) and does solve exactly that — it was never scoped to keep the live DB's workflows active across a migrate, and structurally cannot, since migrate legitimately imports whatever the checked-in fixture says. Fixed for this session by reactivating all 3 via `.save()` (not raw `db.set_value()`, per standing cache-invalidation policy — same method used in every prior occurrence of this quirk, e.g. v1.10), independently reconfirmed via fresh SQL. **Not permanently closed here, deliberately**: a real fix would need a separate `after_migrate()` step that unconditionally reactivates these 3 workflows in the live DB regardless of what the fixture says — clean separation between "what git tracks" (inactive, for diff hygiene) and "what the live site runs" (active, always). Recorded as a known, recurring manual step (reactivate workflows after any migrate) for now, since building that permanent fix wasn't part of this task's scope — a reasonable next task, not undertaken here without being asked. |
| 2026-08-27 | Found & Fixed (real root cause, general mechanism) | The desk-grid alignment complaint on `RFQ Comparison Sheet Item` persisted even after the column-width fix (v1.23) — a second, independent cause was suspected: `price_score`/`lead_time_score`/`weighted_mark` are plain `Float` fields with no explicit `precision` set | **Mechanism, general to any Float/Currency field in Frappe, not specific to this doctype:** without an explicit `precision` value, Frappe stores and displays a Float exactly as computed, trimming trailing zeros per-value rather than rendering every row to a fixed number of decimal places. Two numbers in the same column can therefore have genuinely different string lengths purely from their own arithmetic (e.g. a value that happens to compute to a whole number, `88.0`, next to one that doesn't, `68.57`) — nothing to do with column width, currency, or table structure, all previously investigated and correctly ruled out (v1.23's Known Issues entry, above). Native ERPNext Currency/Float columns almost always set an explicit `precision` (globally via System Settings' default, or per-field), which is why they read as visually uniform and this class of misalignment doesn't normally surface. **Confirmed directly against real data before fixing, not assumed:** `RFQCS-2026-00001`'s actual `weighted_mark` values printed as `88.0` (MA Inc.), `68.57` (Zuckerman Security Ltd.), `53.33` (Summit Traders Ltd.) — a genuine 1-vs-2 decimal-place difference in the same column, and `price_score` showed the same pattern (`80.0`/`100.0` vs `88.89`). **Fixed** by setting `precision=2` on all 3 fields directly on the DocType (`frappe.get_doc("DocType", "RFQ Comparison Sheet Item")`, per-field `precision = "2"`, `.save()`), forcing every row to render to exactly 2 decimal places regardless of its own computed value. Verified the setting took effect via a fresh `get_meta()` read after `clear-cache`, not just an error-free `save()`. Fixture-exported cleanly (already covered by the existing `DocType` fixture filter) and a subsequent `migrate` confirmed clean (exit 0, no orphans). **General lesson for future custom doctypes in this project:** any new Float or Currency field intended for tabular/grid display should set an explicit `precision` at creation time, not just when a display bug is later reported — this class of bug is invisible in a single-record form view and only surfaces once multiple rows with differently-shaped numbers are compared side by side in a grid. |
| 2026-08-27 | Built (future-proofing, not a bug fix) | Genuine ellipsis+tooltip robustness for `RFQ Comparison Sheet Item`'s Supplier grid cell, flagged as an open item in the entry directly below rather than built at the time | Investigated Frappe's real, current grid formatter API via source before writing any JS, rather than guessing from memory or a possibly-outdated pattern: confirmed `frm.fields_dict['items'].grid.update_docfield_property('supplier', 'formatter', fn)` is genuinely correct against this version — `update_docfield_property` (`frappe/public/js/frappe/form/grid.js`) sets `docfield[property]` on both per-row and parent docfields, and Link-field grid rendering explicitly checks `frappe.meta.docfield_map[df.parent][df.fieldname].formatter` before applying it (`_apply_custom_formatter`, `frappe/public/js/frappe/form/formatters.js`) — this is core Frappe's own documented mechanism, confirmed via an inline example comment in the source itself, not assumed. Built as a `Client Script` (`RFQ Comparison Sheet Item Supplier Ellipsis`, Form view on `RFQ Comparison Sheet`, enabled) rendering the cell as a fixed-width, `overflow:hidden; text-overflow:ellipsis; white-space:nowrap` div with a `title` attribute carrying the full value. Hit one new, genuine finding: `Client Script`'s `autoname` is `Prompt` (confirmed via `SELECT autoname FROM tabDocType WHERE name='Client Script'`) — the first insert attempt failed with `ValidationError: Please set the document name` until an explicit `name` was supplied in the insert dict; this is the first `Client Script` created anywhere in this project. Fixture-tracked via a new `"Client Script"` entry in `hooks.py` (first script ever tracked in this app) — `export-fixtures` correctly produced `client_script.json`, confirmed via direct read, and a subsequent `migrate` completed cleanly (exit 0, no orphan doctypes). **Explicitly not a fix for a current bug**: real data today doesn't overflow the 220px Supplier column (v1.23's fix) — `Zuckerman Security Ltd.` at 23 characters fits comfortably. This is deliberate future-proofing against a longer name arriving later. **Limitation, honestly noted**: server-side checks (fresh SQL confirming the script's stored fields, brace-balance and length sanity checks on its JS body) can confirm the script is correctly persisted and syntactically well-formed, but cannot verify the actual visual behavior (ellipsis truncation on overflow, tooltip appearing on hover) — that requires Yasser to open the doctype in-browser and confirm visually; not yet done as of this entry. |
| 2026-08-27 | Found & Fixed (root cause confirmed against real data) | The `in_list_view` fix's own Supplier column (150px) still showed row-to-row alignment inconsistency, again found by Yasser hands-on in the desk | Investigated the most plausible cause first rather than guessing: checked whether the 3 test quotations behind `RFQCS-2026-00001` used differing currencies (which could misalign right-aligned currency cells even at identical widths) — refuted by real data, all 3 genuinely use `JOD`/`KCSC`, no inconsistency there. Also checked `quoted_price`'s own Currency-field `options` — found it has none (no per-row currency-code reference at all), a real but separate observation that doesn't explain row-to-row variance since it's a uniform fallback. **Actual confirmed cause:** `Zuckerman Security Ltd.` (23 characters) genuinely exceeds the 150px Supplier column width, causing that specific cell to overflow and throw off alignment — confirmed against real Supplier data (`CHAR_LENGTH(supplier_name)`), not assumed. **Fixed** by widening the column to 220px, sized with real headroom above the longest actual name on this site, not just enough for today's 3 test suppliers. **Also checked defensively, not just the specific complaint:** the Item column's `item_code` (what the grid genuinely displays, a Link field showing the item's code) is uniformly 6 characters across all real Items (`SKU0xx`-style) — `item_name` (the more descriptive field, up to 10 characters, checked for completeness even though it's not what's actually rendered in this grid) is also comfortably short. Neither approaches the current 140px width; left unchanged, since widening it would have been unjustified by any real data. **Gap flagged, not silently built or silently skipped:** the task's own framing described "CSS overflow handling (ellipsis truncation with a hover tooltip)" as part of a durable fix, but the concrete steps only ever widened the column — confirmed Frappe's grid CSS has a `.grid-overflow-ellipsis` class available (`common/grid.scss`) but it's not applied automatically anywhere in the JS source, so genuine ellipsis+tooltip robustness would require a separate Client Script, which was not built here since it wasn't part of the actual steps given — a real gap for a future session to decide on deliberately, not an oversight. |
| 2026-08-27 | Found & Fixed (desk UX) | `RFQ Comparison Sheet Item`'s desk grid showed nothing but row numbers — no `in_list_view` was ever set on any field when the doctype was created in Stage C | Found by Yasser opening the doctype directly through the desk, not via the portal (proc_portal's own comparison page — Stage D — was unaffected, since it reads the data directly via `get_comparison_sheet()`, not the desk grid's rendering). Set `in_list_view=1` with sensible column widths on all 8 meaningful fields (`item_code` 140, `supplier` 150, `quoted_price` 110, `lead_time_days` 100, `price_score` 100, `lead_time_score` 110, `weighted_mark` 110, `item_rank` 80), confirmed via a fresh `get_meta()` call, not just that `save()` didn't error. Also marked `price_weight`/`lead_time_weight`/`generated_by`/`generated_on`/`request_for_quotation` read-only on the parent — these are generated/copied values at creation time, not meant for manual desk editing after the fact; `naming_series` and the `items` table itself deliberately left editable. Checked the parent's field order while there: `items` was already last, after the metadata fields — already sensible, no reordering needed. Re-confirmed the existing real record (`RFQCS-2026-00001`) still loads correctly with all 3 rows intact after both changes. |
| 2026-08-27 | Found & Recovered (Stage C test setup) | Building real contrasting test data for the scoring engine hit 2 genuine ERPNext domain constraints, not code bugs — both required careful recovery, not blind force-fixes | **(1)** `Supplier Quotation.on_submit()` requires the submitting supplier to actually be listed in the RFQ's own `suppliers` child table (`update_rfq_supplier_status()` throws `"Supplier X not found in RFQ"` otherwise) — a correct rule, since `PUR-RFQ-2026-00003` (from RFQ Stage A) only ever invited `Zuckerman Security Ltd.`, and the two new test suppliers (`MA Inc.`, `Summit Traders Ltd.`) were never added. Console continuation masked the real effect: `docstatus` had already been persisted to `1` before the failure (the same partial-submit pattern seen once before with a Stock Reconciliation, `PROC_APP_SPEC.md` Changelog v1.18) — confirmed via fresh SQL, not the console's own misleading state. **(2)** Fixing that by appending to the RFQ's `suppliers` table via `.save()` hit a second wall: the RFQ itself is submitted (`Approved`), and `suppliers` has no `allow_on_submit`, so `UpdateAfterSubmitError` blocked the normal save path — and the console again printed a misleading "success" from the in-memory object, not what was actually persisted (verified via fresh SQL: genuinely still only 1 supplier). **Recovery:** inserted the two missing `Request for Quotation Supplier` child rows directly via the Document API (bypassing the parent's own submitted-state restriction, a legitimate approach since child-row inserts don't go through the parent's `update_after_submit` validation), then manually called `update_rfq_supplier_status(1)` on both already-submitted Supplier Quotations to complete what their own `on_submit()` couldn't finish. Both fixes independently reconfirmed via fresh SQL. **General lesson, consistent with 2 prior incidents this project:** any RFQ/quotation test script must add invited suppliers before creating quotes from them, and the console's own printed "success" for an in-memory object is not proof of persistence — always re-verify via a fresh, independent query. |
| 2026-08-27 | Decision, Now Documented | RFQ Comparison Sheet's weighted-scoring formula, confirmed directly with Yasser on 2026-08-24 (before Stage A's build began) but never recorded in any spec file until now — closes the gap flagged in the entry directly below | For each RFQ item, among all suppliers who quoted that specific item: `price_score` = (lowest quoted price among those suppliers ÷ this supplier's price) × 100 — the cheapest supplier scores 100, others scale down proportionally. `lead_time_score` = (shortest lead time among those suppliers ÷ this supplier's lead time) × 100 — the fastest supplier scores 100, others scale down proportionally. `weighted_mark` = (price_score × price_weight%) + (lead_time_score × lead_time_weight%). `price_weight` and `lead_time_weight` are set once per RFQ at creation time (Stage B, not yet built) and must sum to 100. Maximum possible `weighted_mark` is 100 (a supplier winning on both factors for that item). Recorded now specifically so future sessions building the comparison/scoring UI don't have to re-derive or re-confirm it. |
| 2026-08-27 | Open Item, Not Fabricated | RFQ Comparison Sheet's weighted-scoring formula (referenced as "confirmed" in a task prompt starting Stage A) does not actually exist anywhere in this project's documentation | **Resolved by the entry directly above (2026-08-27, same day)** — the formula was genuinely confirmed with Yasser on 2026-08-24, just never written down until now; this entry is kept as-is rather than rewritten, since it correctly documents that the gap was real at the time it was flagged, not a false alarm. Searched `PROC_APP_SPEC.md`, `PROC_SUITE_ARCHITECTURE.md`, and `SUPPLIER_PORTAL_SPEC.md` for any prior mention of a weighted-scoring formula — zero matches. Rather than inventing one to satisfy the task's phrasing, this was flagged honestly at the time. Stage A itself (capturing `lead_time_days`/`expected_delivery_date`) did not depend on the formula and was built regardless. |
| 2026-08-23 | Found & Fixed | A `Report` document has its OWN `Has Role` permission gate, entirely separate from `frappe.has_permission(ref_doctype, "report")` — "Physical Count Variance" blocked `operations.officer.test` with `PermissionError: You don't have access to Report` despite the `report` ptype being correctly granted on `Stock Reconciliation` | Traced via `frappe/core/doctype/report/report.py`'s `is_permitted()`: it checks the Report document's own `roles` child table (`Has Role`) — if any rows exist, only those roles may view the report, regardless of the ref_doctype's own DocPerm state. Root cause: Frappe auto-populates this list from whichever roles had *any* DocPerm on the ref_doctype *at report-creation time* — a one-time snapshot, not a live check. Since `Stock Reconciliation` only had `Stock Manager` natively before this task's own permission grant (which ran *after* the report was created, matching the task's own step order), the snapshot captured only `Stock Manager`. The earlier two reports (`Purchase Requisition Status`, `Items Below Reorder Level`) never hit this because their ref_doctypes already had rich role structures from much earlier in this project. **Fixed** by explicitly appending `Department Officer`/`Procurement Officer` to the report's own `roles` table directly. **General lesson for any future custom Query Report:** after granting the `report` ptype on the ref_doctype, always separately check (and if needed, fix) the new Report document's own `Has Role` list — granting ref_doctype permission alone is not sufficient if the report was created before that permission existed. **Definitively closed (2026-08-23), not left as an open question:** re-checked all 3 reports' actual `roles` tables directly — all non-empty, all correctly include the roles that need access. Re-read `is_permitted()`'s source in full: an *empty* `roles` table is itself a genuine, working fallback (`if not allowed: return True` — confirmed by the function's own docstring, "Return True if Has Role is not set or the user is allowed"), not a lockout; this just never applied here since none of the 3 tables were empty. The first two reports were never at risk — not due to luck, but as a deterministic consequence of their ref_doctypes' permission grants (Material Request, Item) predating these reports' creation by weeks, unlike Stock Reconciliation's grant, which was created in the same task as its report. No further fix needed for any of the 3. |
| 2026-08-23 | Found & Recovered | A real, broken document was created during this task's own live testing: the first `Stock Reconciliation` test attempt hit the same ambient-company-default bug found twice before in this project (`proc_portal`'s `create_request()`, RFQ's `submit_quotation()`) — but `bench console`'s continue-past-exception behavior masked it, printing a misleading "Created and submitted" line for a document whose `submit()` had actually failed with `InvalidWarehouseCompany` | Investigated rather than trusted: a fresh SQL query (independent of the console session) showed the document sitting at `docstatus=1` despite the failure — its backing Stock Ledger Entries had been automatically cancelled by ERPNext's own partial-failure handling (confirmed via `is_cancelled=1` on both), and `Bin.actual_qty` was confirmed unaffected (still `0`), so no real stock data was corrupted, only an inconsistent document record remained. A first `cancel()` attempt also failed with the identical error, since cancelling likewise requires the same company/warehouse validation — confirmed this explicitly rather than assuming force-delete was safe on an unresolved `docstatus=1` document (per this project's own established discipline). **Recovery:** corrected the document's `company` field directly via `frappe.db.set_value` (a minimal, understood fix, not a full re-validation), then `cancel()` succeeded cleanly (`docstatus=2`, confirmed via fresh SQL), then force-deleted. Redid the actual test correctly afterward, setting `company` explicitly from the start (derived from the warehouse, matching the established fix pattern from the two prior occurrences of this bug class) — this time `insert()`/`submit()` both succeeded cleanly on the first attempt. |
| 2026-08-23 | Real Test Data (Not to Clean Up) | `SKU010`'s reorder level at `Stores - K` was set to `10` (current actual stock: `0`), specifically to prove `Items Below Reorder Level` genuinely triggers | This is a deliberate, permanent test configuration demonstrating a real feature working end-to-end, not a leftover artifact — do not delete or reset it in a future session without a specific reason to. |
| 2026-08-23 | Structural, Recurring Gotcha — Now Permanently Mitigated | Any `bench export-fixtures --app proc_app` run silently re-drifts all 3 workflows' fixture `is_active` back to `1`, even when the export is for something completely unrelated | Confirmed via source, not assumed: `bench migrate`'s own `sync_fixtures()` (`frappe/utils/fixtures.py`) actually *imports* fixture files into the DB — the opposite direction from what "migrate reverts is_active" initially suggested. The real cause is `export-fixtures` itself: since `Workflow` is one filtered doctype among several in the same `hooks.py` fixtures list, running `export-fixtures` for ANY reason (e.g. capturing a brand-new Report) re-exports ALL filtered `Workflow` records with their *current live* `is_active` value — which is `1`, since all 3 are kept active for ongoing cross-session testing. This recurred twice in one session (Purchase Requisition Status build, then Items Below Reorder Level build) before being addressed structurally rather than patched manually a third time. **Fix (2026-08-23): a tracked git pre-commit hook** — `.githooks/pre-commit` — automatically resets any `is_active: 1` found in `fixtures/workflow.json` back to `0` and re-stages the file before every commit completes, formatted to exactly match `bench export-fixtures`'s own output (`indent=1`, no trailing newline — confirmed via a byte-for-byte round-trip check before writing the hook, to avoid the hook itself introducing spurious formatting diffs). Verified live: deliberately drifted all 3 workflows to `is_active: 1` in the fixture file, staged it, committed — the hook fired, printed its correction message, and the actual committed blob (checked via `git show HEAD:...`, not just the working tree) confirmed all 3 back to `0` with correct formatting preserved. **Real, standing limitation, not fully closed by the hook alone:** git hooks live outside `.git/` only when `core.hooksPath` is explicitly configured to point at the tracked `.githooks/` directory — this is a **one-time, per-clone setup step** (`git config core.hooksPath .githooks`), not something that ships automatically with a fresh `git clone`. Anyone (or any future session) working from a new clone of this repo must run that command once before the protection applies. |
| 2026-08-23 | Found, Data-Honesty Note | `Material Request Item.rate`/`.amount` were checked before building "Purchase Requisition Status" — found populated in 8/8 existing rows, the opposite of the expected "rarely populated at requisition stage" premise | Investigated rather than assumed: this is very likely ERPNext's own automatic buying-price-list lookup at insert time (the same "Item rates have been updated based on the selected Buying Price List" behavior observed during earlier Material Request testing), not a value requesters deliberately enter — so the report's "Estimated Value" column reflects whatever price list rate happens to be configured for an item, not a negotiated or approved procurement figure. Also flagged: n=8 is far too small a sample (all this project's own test data) to be a reliable signal about real production usage either way. Built the report as originally specified regardless, since the data genuinely is populated — but this caveat should inform how "Estimated Value" is labeled/explained to end users, not treated as a firm budget number. |
| 2026-08-23 | Found & Confirmed | Query Reports require a distinct `"report"` permission type, separate from read/write/create/submit — `Procurement Officer` had `read=1` on `Material Request` but not `report=1`, so "Purchase Requisition Status" raised a `PermissionError` on first run | Traced via source (`frappe/desk/query_report.py`'s `_run()`): `if not frappe.has_permission(report.ref_doctype, "report"): frappe.msgprint(..., raise_exception=True)` — this checks a `report` ptype against the report's `ref_doctype` (`Material Request`), not the `Report` doctype itself, and genuinely blocks execution (not just a warning). Confirmed via a fresh `Custom DocPerm` query that `report=0` for Procurement Officer despite `read=1`. Fixed via a new `setup_report_permissions()` in `setup.py`, following the file's established idempotent pattern, granting `report=1` for Procurement Officer on Material Request specifically. **General lesson for any future custom Query Report in this project:** granting `read` on the `ref_doctype` is not sufficient on its own — the `report` ptype must be explicitly granted too, or the report will raise a `PermissionError` for any role that only has the standard CRUD ptypes. Also found: the new `Report` document itself was never fixture-tracked — `Report` wasn't yet in `hooks.py`'s `fixtures` list, so it would have existed only on this site's database, not survived a fresh install. Added a `Report` fixture filter and re-exported; confirmed captured in `fixtures/report.json`. |
| 2026-08-23 | Found & Fixed (3 real bugs, one integration test) | RFQ Stage C's real integration test of `submit_quotation()` — a feature "done" since Phase 2, never once exercised with real data — found 3 genuine, previously-invisible bugs in a single pass | **Bug 1 — missing warehouse:** identical class of failure to the RFQ-creation test-script gap above, but this time in the real production endpoint: a supplier has no way to know or provide the buyer's internal warehouse, yet ERPNext requires one per stock item row on `Supplier Quotation`. **Fixed** by auto-filling from `Item Default`'s per-company `default_warehouse` (same mechanism already proven in `proc_portal`'s New Request form), invisible to the supplier; throws a clear, actionable error (`"Item {code} has no default warehouse configured for {company}. Please contact procurement..."`) instead of ERPNext's raw validation message if genuinely none exists — verified both paths live. **Bug 2 — wrong company:** fixing Bug 1 surfaced a second failure, `Warehouse Stores - K does not belong to company KCSC (Demo)` — the new `Supplier Quotation` never had its own `company` field set explicitly, so ERPNext's ambient-default resolution picked the wrong one of this site's two companies. **The exact same class of bug already found and fixed once before** in `proc_portal`'s `create_request()` (`PROC_PORTAL_SPEC.md` Known Issues, 2026-08-13) — same root cause, same fix: derive `company` explicitly (from the RFQ itself, already fetched for Bug 1's lookup) rather than trusting ambient context. **Bug 3 — quote_status never syncing:** after fixing Bugs 1-2, the Supplier Quotation was created successfully but the RFQ's `quote_status` for the acting supplier never flipped to "Received". Root-caused via ERPNext core source (`erpnext/buying/doctype/supplier_quotation/supplier_quotation.py`): the status-sync logic (`update_rfq_supplier_status()`) only runs from `on_submit`/`on_cancel` — and `submit_quotation()` only ever called `.insert()`, leaving the document permanently in Draft. This was a genuine design decision, not an obvious one-line fix (submitting on the supplier's behalf makes the document no longer editable) — confirmed with the user before implementing: `sq.submit()` added after insert, since a supplier's quotation submission is final from their side and matches ERPNext's own expected doctype lifecycle. **A 4th, separate finding, clarified as a test-methodology gap rather than a code bug:** even after all 3 fixes, `quote_status` still didn't flip on the first corrected retest — traced to the test payload never including each item's own `Request for Quotation Item` child-row `name`, which `update_rfq_supplier_status()`'s matching logic requires to link a specific quote line back to a specific RFQ line. `submit_quotation()`'s existing code already threads this through correctly (`"request_for_quotation_item": item.get("name")`) — the real `supplier_portal` frontend (`rfq_detail.html`) already reads this `name` from the RFQ's own server-rendered item data and passes it through naturally; only the ad-hoc test scripts (Stage C's original and this fix's initial retest) omitted it. Retested with the RFQ item's real row name included — `quote_status` correctly flipped to "Received". All 4 findings verified end-to-end via real HTTP + fresh SQL, not assumed; test artifacts cleaned up, `quote_status` reset to `Pending` for a clean Stage B slate. **Follow-up (2026-08-27) — the same root cause's second real consequence, now fixed:** Stage C's 3 lasting test quotations (`PUR-SQTN-2026-00005/6/7`, deliberately kept for Stage D's UI testing rather than cleaned up) turned out to carry this exact gap themselves — their `Supplier Quotation Item` rows all had `request_for_quotation_item = NULL`, since they were created by an earlier ad-hoc console script predating this test-methodology finding. Consequence: `quote_status` for all 3 real suppliers on `PUR-RFQ-2026-00003` was genuinely stuck at `Pending` (not a display bug — confirmed via fresh SQL and by loading the real `proc_portal` page as `procurement.officer.test`, checking for the literal button text `"Generate/View Comparison"` rather than the looser, initially-misleading substring `"Comparison"` which also matches the page's always-present JS method name), which correctly hid the portal's "Generate/View Comparison" button, since its Jinja gate (`rfq-detail.html`) legitimately requires at least one `Received` row. **Fixed as pure data recovery, no code change**: looked up the real RFQ Item row (`ib7s42mkmj`, the `SKU010` line on `PUR-RFQ-2026-00003`), set `request_for_quotation_item` on all 3 Supplier Quotation Items to reference it, then re-ran `update_rfq_supplier_status(1)` on each quotation — the same recovery mechanism already used once before in this same entry. Verified via fresh SQL (`quote_status` now `Received` for all 3) and by reloading the real portal page as the same test user (button text now genuinely present). Also verified `RFQCS-2026-00001`'s existing scored comparison data was unaffected throughout, not assumed: `generate_comparison_sheet()` queries `Supplier Quotation Item` by `request_for_quotation` (a different, always-correct field), never by `request_for_quotation_item`, so this gap never touched the scoring engine itself — re-confirmed identical rankings/marks (`MA Inc.` 88.0/1, `Zuckerman Security Ltd.` 68.57/2, `Summit Traders Ltd.` 53.33/3) before and after the fix. |
| 2026-08-23 | Found, Not a Bug | RFQ Approval workflow's first live test failed twice before succeeding — both real ERPNext validation requirements specific to `Request for Quotation`, not workflow/permission problems | First attempt: `ValidationError: Row #1: Warehouse is mandatory for stock Item SKU010` — `erpnext.buying.utils.validate_stock_item_warehouse()` requires a `warehouse` per item row for any stock item, a stricter rule than Material Request's item warehouse field (confirmed earlier this project to be genuinely optional at the ERPNext core level). Second attempt (after adding `warehouse`): `ValidationError: Row #1: UOM Conversion Factor is mandatory` — `BuyingController.set_qty_as_per_stock_uom()` requires an explicit `uom` (with a resolvable `conversion_factor`) per item row. Both are real, pre-existing ERPNext constraints on RFQ Item rows, unrelated to anything built today — confirmed by the traceback originating entirely in `erpnext/buying/utils.py`/`erpnext/controllers/buying_controller.py`, not any of this project's own code. Neither failed insert was actually persisted (confirmed via fresh SQL both times — `bench console`'s continue-past-exception behavior meant later cells printed a pre-allocated `doc.name` as if creation had succeeded, another instance of the documented console-testing gotcha). Fixed the *test script*, not the doctype/workflow: the third attempt included `warehouse`, `uom`, and `conversion_factor` explicitly and succeeded end-to-end. **General lesson for any future RFQ-creation code (including proc_portal's upcoming creation screens):** item rows need `warehouse` and `uom`/`conversion_factor` set explicitly — don't assume Material Request's more permissive item-row rules carry over to RFQ. |
| 2026-08-23 | Confirmed Acceptable | Department Officer + Stock User can create Material Requests | Found via live permission testing in `proc_portal`: any user holding both Department Officer (our custom role, review/approve only by design) and ERPNext's native Stock User role (granted separately for the Issue-from-Stock flow) can ALSO create Material Requests, since Stock User natively has `create=1` on Material Request (a pre-existing OOB grant, not something this project added). Permissions are additive across all of a user's roles, so this combination genuinely allows creation, deviating from the original Department Officer role design (review/approve only). Confirmed with Yasser (2026-08-23): this is acceptable, reasonable real-world behavior — warehouse/stock staff plausibly do need to raise their own material requests too. Not tightened. Documented so this isn't mistaken for an oversight in future review. |
| 2026-08-22 | Deliberate Scope Decision | Purchase Order Amendment approval has no automated effect | Confirmed via hands-on desk testing that reaching "Approved" state is purely a documentation/internal-approval record — it does not update the linked Purchase Order's actual fields, and does not notify the supplier. This was flagged directly to Yasser during testing; decision made to leave as documentation-only for now rather than build automation ahead of real, workshop-confirmed business rules for exactly how amendments should apply. Future work: (1) auto-update PO fields matching the amendment's description once approved, (2) trigger a supplier notification (ties into the still-unbuilt Notification Master List items documented in `PROC_SUITE_ARCHITECTURE.md`). |
| 2026-08-22 | Found & Fixed | "Insufficient permission for Purchase Order" surfaced testing the new Purchase Order Amendment form — Procurement Officer had no permission to select a Purchase Order on the form's `purchase_order` link field | Scoped specifically to the PO Amendment feature — Material Request approval never needed Procurement Officer to read Purchase Order directly. Confirmed no existing `Custom DocPerm` covered this combination beforehand (ruling out something else masking the real cause). Fixed by extending `setup_po_amendment_permissions()` in `setup.py` with an idempotent check-before-create grant of `read=1` on `Purchase Order` for `Procurement Officer`, alongside the PO Amendment doctype's own permissions in the same function — consistent with the file's existing pattern rather than adding a separate function for a single permission. Verified via `Custom DocPerm` query and a clean `migrate` (exit 0, no orphans). |
| 2026-08-14 | Found & Fixed | "Insufficient permission for Price List" surfaced for the first time testing the actual desk New Material Request form | The desk form renders every field's dropdown directly, including `Price List` (used internally for item rate lookups) — something portal/API testing never exercises, since `proc_portal`'s own New Request form doesn't render or need a Price List field at all. Confirms an expected pattern, not a one-off: the desk UI surfaces supporting-doctype permission gaps that portal/API testing structurally cannot, since it renders every linked field's dropdown, not just the fields our own code actually touches. Fixed by adding `"Price List"` to the existing `SUPPORTING_DOCTYPES` list in `setup.py` (same read-only, 4-role pattern already covering Item/Item Group/UOM/Warehouse/Brand/Company/Department) rather than a one-off fix — reproducible on any future install via `after_migrate()`. Verified: all 4 `SUPPORTING_ROLES` now have `read=1` on `Price List`; the pre-existing native-role core `DocPerm` entries (`Purchase User`, `Sales User`, `Sales Master Manager`, `Manufacturing User`, `Purchase Master Manager`) were confirmed unaffected (same Custom-DocPerm-mirroring behavior already documented for the Stock Settings fix, checked again rather than assumed). |
| 2026-08-14 | Found & Fixed | `operations.officer.test` hit "No permission for Stock Settings" creating a Stock Entry from an "Approved - Issue" Material Request, found via hands-on desk testing | A genuine ERPNext OOB gap, not a lost role or a broken custom permission — confirmed methodically before fixing. `frappe.get_roles()` confirmed `Stock User` was still present (ruling out role loss). Core `tabDocPerm` for `Stock Settings` grants read only to `Sales User` and `Stock Manager` — **not** `Stock User`, even though Stock Entry's creation flow reads Stock Settings internally for defaults (negative-stock allowance, price-list-rate behavior, etc.). No `Custom DocPerm` existed for `Stock Settings` at all beforehand, confirming this predates any of our own permission work. **Fixed narrowly**: granted `read=1` on `Stock Settings` to `Department Officer` only, via a new `setup_stock_settings_permission()` function in `setup.py` (same idempotent pattern as every other permission fix in this file), rather than modifying the native `Stock User` role globally — avoids any unaudited system-wide effect on other `Stock User` holders outside this app's scope. One side effect worth recording, not a problem: adding the first `Custom DocPerm` for a doctype makes Frappe use `Custom DocPerm` exclusively for that doctype going forward, so Frappe automatically mirrored the pre-existing `Sales User`/`Stock Manager` core permissions into `Custom DocPerm` too — confirmed their original core `DocPerm` rows are untouched and their effective access is unchanged, only `Department Officer` gained anything new. |
| 2026-08-14 | Found & Fixed | Creating the `Purchase Order Amendment` DocType via the Document API failed with `ValidationError: Cannot set Assign Submit if not Submittable` | The DocType dict included a permissions entry with `"submit": 1`, but didn't set `"is_submittable": 1` on the DocType itself — required for any doctype using Submit/Cancel/docstatus workflow transitions. Frappe correctly rejected the insert before persisting anything (confirmed via a fresh DB check — zero rows created). Fixed by adding `"is_submittable": 1` to the DocType dict; retry succeeded. **General lesson:** any workflow-driven custom DocType created via the Document API (not the Desk UI, which sets this automatically when you tick "Is Submittable") needs `is_submittable: 1` set explicitly. |
| 2026-08-14 | Found, Not Fixed (out of scope) | 2 pre-existing negative stock quantity anomalies found on this dev site during Stock Management testing, unrelated to anything being tested | Not investigated further or fixed — flagged here so a future session doesn't rediscover this from scratch. Needs its own investigation before considered a real issue vs. dev-data artifact. |
| 2026-08-14 | Process Lesson | bench console testing pitfalls found during Stock Management verification | (1) Writes made in a bench console session are NOT automatically committed — if the session exits (or a script ends) before an explicit `frappe.db.commit()`, those writes silently vanish, which can make a test look like it failed when it actually just wasn't persisted. Always call `frappe.db.commit()` explicitly before relying on a write's effects in a later step. (2) A cleanup script that calls `doc.cancel()` then unconditionally proceeds to force-delete, without checking whether `cancel()` actually succeeded, can end up force-deleting a document that's still genuinely submitted (docstatus=1) if `cancel()` silently failed or was skipped — always verify `cancel()` succeeded (check docstatus afterward) before force-deleting, especially for documents with real linked stock/ledger impact. |
| 2026-08-03 | Scaffolding | App created as `karama_proc` | `bench new-app karama_proc`, installed on site1.local. No custom code yet. |
| 2026-08-03 | Rename | Renamed `karama_proc` → `proc_app` (App Title: KCSC Proc) | Decided before any real content was added. Since the app was still an empty shell, executed as uninstall + folder removal + fresh `bench new-app proc_app`, rather than an in-place rename — avoids any risk of leftover references. |
| 2026-08-03 | Planned | Future task: split cross-app architecture content into `PROC_SUITE_ARCHITECTURE.md` | Sections 16-18 of `SUPPLIER_PORTAL_SPEC.md` (App Boundary & Ownership, OOB-vs-Custom Decision Log, Notifications Master List, Roles & Permissions) will be relocated to a new `PROC_SUITE_ARCHITECTURE.md` living in this app's repo. Decided to do this before Phase 1 (doctype migration) begins, not after. Not yet started. |
| 2026-08-06 | Complete | Cross-app architecture content relocated to `PROC_SUITE_ARCHITECTURE.md` | The task planned on 2026-08-03 (row above) is done. Old Sections 16-18 of `SUPPLIER_PORTAL_SPEC.md` were moved into this app's repo as `PROC_SUITE_ARCHITECTURE.md`, renumbered as Sections 1-3 there. All internal cross-references within the moved content were renumbered to match; 8 external cross-references remaining in `SUPPLIER_PORTAL_SPEC.md` were updated to point at the new location. Completed before Phase 1 (doctype migration) began, as planned. |
| 2026-08-14 | Found & Fixed (two rounds) | `bench migrate` silently deleted the 3 just-moved doctypes as "orphans" (exit code 0, no error text) | A new Module Def ("Proc App") was created in the DB without adding it to `apps/proc_app/proc_app/modules.txt` first — migrate's doctype-discovery scan never recognized the moved files as belonging to a valid module, and its orphan-cleanup pass deleted the now-unreachable `tabDocType` records for all 3 doctypes (underlying DB tables were never dropped, confirmed via `SHOW TABLES`; 0 rows existed in any of them anyway, so no data was actually lost). Caught by explicitly checking the migrate log's "Removing orphan doctypes" section, not just grepping for the literal words "Error"/"Traceback"/"Failed" — an exit code of 0 does not mean nothing significant happened. **First fix** (adding `Proc App` to `modules.txt`) was necessary but not sufficient: a second migrate still didn't register the doctypes, and `proc_app` didn't even appear in migrate's "Updating DocTypes for X" sync list at all. **Second fix**, root-caused via `frappe.scrub()`/`get_module_path()` rather than guessed: module "Proc App" scrubs to `proc_app`, and Frappe expects that module's doctypes at `apps/proc_app/proc_app/proc_app/doctype/...` (a third nesting level, mirroring `supplier_portal/supplier_portal/supplier_portal/`'s existing pattern) — the files had been placed one level shallower. Moved them one level deeper to match. A third migrate then correctly registered all 3 doctypes under module "Proc App" with zero orphans, confirmed via `tabDocType` query and a real functional smoke test (create + delete) for both `Supplier ASN` and `Supplier Invoice Dispute`. **General lesson:** when creating a new Module Def for an app, always add it to that app's `modules.txt` in the same step, and remember that a module's expected doctype folder path is `get_module_path(module_name)` — i.e., `<app_package>/<scrub(module_name)>/doctype/`, not necessarily matching whatever nesting depth was assumed by analogy to a different app. |

---

## 5. Changelog

| Version | Date | Author | Summary |
|---|---|---|---|
| 1.30 | 2026-08-27 | KCSC | Proved the multi-supplier PO grouping logic flagged as an open coverage gap in v1.29, with real, deliberately adversarial data: a fresh 2-item RFQ (`SKU010`, `SKU009`) inviting `MA Inc.` and `Summit Traders Ltd.`, with reversed price advantage per item (MA Inc. cheaper on `SKU010` at 50 vs 150; Summit Traders cheaper on `SKU009` at 80 vs 200) — specifically to prove per-item competition, not just "one supplier always wins everything." Comparison sheet scoring confirmed the split BEFORE generating POs, per the task's own discipline: `MA Inc.` rank 1 on `SKU010` (`weighted_mark=100.0`), `Summit Traders Ltd.` rank 1 on `SKU009` (`weighted_mark=100.0`) — genuinely different winners per item. **The critical result**: `generate_purchase_orders_from_comparison()` produced exactly 2 Purchase Orders — `MA Inc.`'s containing only `SKU010` (qty 5, rate 50), `Summit Traders Ltd.`'s containing only `SKU009` (qty 3, rate 80) — no cross-contamination, no merged/missing POs, independently re-verified via a fresh SQL join across both documents' item rows, not just the console's own print. The grouping logic (`by_quotation.setdefault(row.supplier_quotation, []).append(...)`) is now genuinely proven, not just reviewed as "looks simple." **A second, unrelated but real environmental blocker was hit and fixed along the way**: `apply_workflow(rfq, "Submit")` failed with `DoesNotExistError: Workflow  not found` — confirmed via fresh SQL that all 3 project workflows (`Material Request Approval`, `Purchase Order Amendment Approval`, `RFQ Approval`) were currently `is_active=0` in the live DB, genuinely blocking any real workflow-based document creation at the time, not specific to this test. Root cause: the prior task's own closing `bench migrate` (run to confirm Stage B's `after_migrate()` change didn't break a clean cycle) re-imported `workflow.json` via `sync_fixtures()` — and since the permanent pre-commit hook (v1.17) always resets the *committed fixture* to `is_active=0` before every commit, that correctly-zeroed fixture gets *re-applied to the live DB* on the very next migrate, deactivating the real workflows. **This is a previously undocumented consequence of the v1.17 mitigation's actual scope**: the hook keeps the git-committed fixture clean (which is correct and working as intended), but does not — and structurally cannot — keep the live DB's workflows active across a migrate, since migrate legitimately imports whatever the fixture says. Fixed by reactivating all 3 via `.save()` (not raw `db.set_value()`, per standing cache-invalidation policy), independently reconfirmed via fresh SQL. Recorded as a known, recurring operational step (re-activate workflows after any migrate), not a new bug and not attempted to be permanently closed here, since doing so wasn't part of this task's own scope. Full detail in Known Issues. |
| 1.29 | 2026-08-27 | KCSC | PO-from-Comparison-Sheet, Stage B: `generate_purchase_orders_from_comparison(rfq_name)` (`proc_app/api/rfq.py`), reusing ERPNext's own native, whitelisted `make_purchase_order()` mapper (confirmed in the prior read-only investigation to already support selective item filtering via `args={"filtered_children": [...]}`) rather than building custom mapping logic. Groups the comparison sheet's rank-1 (winning) rows by `supplier_quotation`, generating exactly one Draft Purchase Order per distinct winning supplier, each containing only that supplier's *actually-won* items — not their whole quotation. Left as Draft (Yasser's decision) for manual review before submission. **Live testing surfaced 3 stacked permission gaps and 1 real data-validation gap, none anticipated by the task's own scope** — found one at a time, each only visible once the previous was fixed, all confirmed via live `PermissionError`/`ValidationError`, not assumed: **(1)** Procurement Officer had `read` but not `create`/`write`/`submit` on `Purchase Order` (the existing grant, from v1.10, was narrowly for selecting a PO on the Amendment form's link field only). **(2)** `get_mapped_doc()` checks read permission on the SOURCE document (`Supplier Quotation`) being mapped from — Procurement Officer had zero access to it at all, confirmed via `DocPerm`/`Custom DocPerm` queries showing no native role or prior grant covered it. **(3)** `PurchaseOrder.insert(ignore_permissions=True)` does not bypass an inline `frappe.throw(exc=PermissionError)` embedded in `AccountsController.set_payment_schedule()`'s `account_perm_check()` — Procurement Officer needed `read` on `Account` too, the same class of gap already found once before for a different role (v1.4, Supplier Portal User). All 3 fixed in a single new `setup_po_generation_permissions()` in `setup.py` (registered in `after_migrate()`), matching the file's established idempotent patterns — delete-then-recreate for the doctype's own primary grant, if-not-exists for the two secondary read-only grants. **(4)** Past all 3 permission gaps, `PurchaseOrder.validate_schedule_date()` then threw `"Please enter the Required By."` — ERPNext's own `get_schedule_dates()` only fills `schedule_date` from a linked `Material Request Item`, which these RFQ-sourced quotations never have. Fixed by explicitly setting each mapped PO Item's `schedule_date` from the winning comparison-sheet row's own already-scored `lead_time_days` (`add_days(nowdate(), lead_time_days)`), falling back to a 7-day default only when lead time itself is genuinely missing — deliberately mirroring the same "no real signal" handling the scoring engine already applies. Verified live end-to-end as `procurement.officer.test` against real data (`PUR-RFQ-2026-00003`/`RFQCS-2026-00001`): exactly 1 PO created (`PUR-ORD-2026-00011`, correctly — only MA Inc. won on this single-item RFQ), `docstatus=0` (Draft), `supplier=MA Inc.`, item `SKU010` at `rate=100`, `schedule_date=2026-08-30` (= `nowdate()` + `lead_time_days=3`), `supplier_quotation`/`supplier_quotation_item` both cross-checked directly against fresh SQL on the real source `Supplier Quotation Item` (`24pjeb916j`) — not just non-null. Test PO cleaned up and independently reconfirmed gone via fresh SQL. `bench migrate` re-run afterward (exit 0, no orphans) to confirm the new `after_migrate()` function doesn't break a clean migrate cycle, and all 3 permission grants independently reconfirmed still correct post-migrate (idempotency check). **Honest coverage gap, not glossed over**: this RFQ has only 1 item, so the "group winning items by supplier, one PO per supplier" logic was only exercised in the trivial single-group case — it has not yet been proven against a multi-item RFQ with mixed winners across 2+ suppliers. Recommended (not yet built): a second test RFQ with multiple items and mixed winners, since this task's live testing already showed code-review-only confidence would have missed all 4 real gaps found here. Full detail in Known Issues. |
| 1.28 | 2026-08-27 | KCSC | PO-from-Comparison-Sheet, Stage A: added `supplier_quotation` (Link → Supplier Quotation, read-only) and `supplier_quotation_item` (Data, read-only, hidden — carries the source `Supplier Quotation Item` row's own `name` for exact line-level traceability) to `RFQ Comparison Sheet Item`, laying the groundwork for Stage B's Purchase Order generation to reference each winning quote's real source document rather than just a supplier/item pairing. Extended `generate_comparison_sheet()` (`proc_app/api/rfq.py`) minimally — added `sq.name as sq_name, sqi.name as sqi_name` to the existing SQL query and 2 new keys to the row dict — no structural rewrite. Verified live against real data (`RFQCS-2026-00001`/`PUR-RFQ-2026-00003`): all 3 rows' `supplier_quotation`/`supplier_quotation_item` cross-checked directly against fresh SQL on `tabSupplier Quotation`/`tabSupplier Quotation Item`, confirming exact correctness (e.g. `MA Inc.` → `PUR-SQTN-2026-00006`, item row `24pjeb916j`, independently confirmed as that quotation's own real `SKU010` line) — not just that the fields were non-null. Fixture-exported (already covered by the existing `RFQ Comparison Sheet Item` `DocType` filter, no `hooks.py` change needed) and a subsequent `migrate` confirmed clean (exit 0, no orphans). Stage B (actual PO generation) not yet built. |
| 1.27 | 2026-08-27 | KCSC | Final resolution of the desk-grid decimal-display inconsistency, closing out an investigation spanning several sessions (v1.23 ruled out column width/currency; v1.26 found and attempted to fix via `precision=2`). v1.26's fix did not actually work: Yasser's screenshot still showed `88` alongside `68.57`/`53.33` in the same column. Root-caused via `frappe/public/js/frappe/form/formatters.js` source: `Float`'s formatter contains a deliberate `// show 1.000000 as 1` block that overrides the field's own `precision` to `0` whenever a value's decimal part is missing or zero (unless the caller passes `options.always_show_decimals`, which grid cell rendering does not) — `Currency`'s formatter has no equivalent override. Considered switching these 3 fields to `Currency` (Yasser's initial decision) specifically to inherit that fixed-precision behavior, but first investigated whether the currency symbol could be suppressed per-field, since a `JOD` label next to a lead-time score would be confusing — confirmed via source (`number_format.js`, `data.py`) and via a live runtime check that `hide_currency_symbol` is a **global, site-wide default only** (`frappe.boot.sysdefaults.hide_currency_symbol` / `frappe.defaults.get_global_default(...)`), with no per-docfield equivalent at all (`DocField`'s own schema has no such field; `System Settings` doesn't either, confirmed by a real `ValidationError` when queried). Flipping it would have hidden currency symbols on every monetary field sitewide — an unacceptable tradeoff, reported honestly before proceeding rather than silently working around it. **Final fix**: fields stay `Float` with `precision=2` (untouched, still correct at the data-model level); extended the existing Supplier-ellipsis `Client Script` with a second formatter block, applying `parseFloat(value).toFixed(2)` to `price_score`/`lead_time_score`/`weighted_mark` via the same `update_docfield_property(fieldname, 'formatter', ...)` mechanism already proven in v1.24 — a pure client-side display fix, no data-model change. **Caught a real placement bug before it shipped**: the originally-proposed string-splice approach (`current_script.rfind("});")`) targeted the *last* `});` in the script, which closes the outer `frappe.ui.form.on(...)` call, not the inner one closing `refresh`'s existing formatter — inserting there would have placed the new code as a bare statement outside the `refresh` function, invalid JavaScript. Confirmed this precisely by computing the spliced string without saving it first: both brace-count and paren-count checks passed anyway (they're insensitive to structural placement), which would have wrongly signaled "OK" — exactly the false-positive risk flagged in this task's own instructions. Rewrote the script cleanly instead, with both formatters correctly nested inside the same `refresh` function; verified with `node --check` against the actual saved document content (not just a local draft) before and after, confirming genuine syntactic validity beyond brace/paren counting. Verified nothing server-side was affected: raw stored values and fieldtype (`Float`) on `RFQCS-2026-00001` unchanged before and after. Fixture-exported (already covered by the existing `Client Script` filter from v1.24) and a subsequent `migrate` confirmed clean (exit 0, no orphans). **Same visual-verification limitation as v1.24**: this can be confirmed server-side as syntactically valid and correctly persisted, but the actual rendered decimal alignment in the desk grid still needs Yasser's visual confirmation in-browser. |
| 1.26 | 2026-08-27 | KCSC | Found and fixed the actual root cause of the recurring desk-grid alignment complaint on `RFQ Comparison Sheet Item` — not the column-width issue already fixed in v1.23 (real, but not the whole story): `price_score`, `lead_time_score`, `weighted_mark` were created as plain `Float` fields with no explicit `precision`, so Frappe displays each value with its own natural (trailing-zero-trimmed) decimal length rather than a uniform one. Confirmed directly against `RFQCS-2026-00001`'s real stored data before touching anything: in the same `weighted_mark` column, MA Inc. displayed as `88.0` (1 decimal) while Zuckerman Security Ltd. and Summit Traders Ltd. displayed as `68.57`/`53.33` (2 decimals) — a genuine, row-to-row text-length difference in a right-aligned numeric column, independently of column width. Fixed by setting `precision=2` on all 3 fields directly on the `DocType`, matching how native ERPNext Currency/Float columns (which always set explicit precision) stay visually uniform. Fixture-exported (already covered by the existing `RFQ Comparison Sheet Item` `DocType` filter, no `hooks.py` change needed) and a subsequent `migrate` confirmed clean (exit 0, no orphans). Full mechanism explained in Known Issues, framed generally — this applies to any custom Float/Currency field in this project, not just these 3. |
| 1.25 | 2026-08-27 | KCSC | Fixed a genuine data-state bug found by investigating why the "View Comparison" button wasn't showing on `PUR-RFQ-2026-00003`'s `proc_portal` detail page: confirmed the button's own gating logic (`rfq-detail.html`'s `quoted_count = rfq.suppliers | selectattr("quote_status", "equalto", "Received") | list | length`) was correctly working, and that all 3 suppliers genuinely had `quote_status = "Pending"` despite 3 submitted `Supplier Quotation` documents existing — traced via ERPNext's own `update_rfq_supplier_status()` source to the same test-methodology gap already documented in Changelog v1.14/Known Issues: Stage C's 3 lasting test quotations (kept deliberately for Stage D's UI testing) had `request_for_quotation_item = NULL` on every item row, since they predated that finding. Fixed as pure data recovery (no code change, since the gating logic and `generate_comparison_sheet()` were both already correct): linked all 3 quotations' item rows to the real RFQ Item row (`ib7s42mkmj`), then re-ran `update_rfq_supplier_status(1)` on each — the same recovery mechanism already used once before in this project. Verified via fresh SQL (`quote_status` now `Received` for all 3) and by reloading the real portal page as `procurement.officer.test`, checking the literal button text `"Generate/View Comparison"` rather than a looser substring match (a mistake specifically avoided this time, after an earlier check in this same investigation matched only the page's always-present JS method name). Also verified `RFQCS-2026-00001`'s existing comparison data was genuinely unaffected throughout — not assumed, since its scoring query uses the unrelated `request_for_quotation` field — rankings/marks identical before and after. Full detail in Known Issues. |
| 1.24 | 2026-08-27 | KCSC | Built the genuine ellipsis+tooltip robustness flagged as an open item in v1.23: a `Client Script` (`RFQ Comparison Sheet Item Supplier Ellipsis`, Form view, enabled) attached to `RFQ Comparison Sheet` that formats the `items` grid's `supplier` cell via `frm.fields_dict['items'].grid.update_docfield_property('supplier', 'formatter', ...)`, rendering it in a fixed-width (`max-width:200px`) `overflow:hidden; text-overflow:ellipsis; white-space:nowrap` div with a `title` attribute carrying the full value. Investigated Frappe's real, current grid formatter API via source before writing any JS, rather than guessing from memory: confirmed `update_docfield_property` (`frappe/public/js/frappe/form/grid.js`) genuinely sets `docfield[property]` on both per-row and parent docfields, and that Link-field grid rendering explicitly checks `frappe.meta.docfield_map[df.parent][df.fieldname].formatter` (`_apply_custom_formatter`, `frappe/public/js/frappe/form/formatters.js`) — this is core Frappe's own documented mechanism, not an undocumented hack. `Client Script`'s `autoname` is `Prompt` (a genuinely new finding this project, first script created here) — first insert attempt failed with `ValidationError: Please set the document name` until an explicit `name` was supplied; retried successfully, independently reconfirmed via fresh SQL. Added `"Client Script"` to `hooks.py`'s fixtures list (new entry, first script fixture-tracked in this app) — `export-fixtures` correctly produced `client_script.json`, and a subsequent clean `migrate` (exit 0, no orphans) confirmed. **This is future-proofing, not a fix for a current bug**: today's real data doesn't overflow the already-widened 220px Supplier column (v1.23) — the longest real name, `Zuckerman Security Ltd.` at 23 characters, fits — but a future, longer supplier name would otherwise silently truncate the grid layout without this. Server-side testing (SQL, sanity brace-balance/length checks) can confirm the script is correctly stored and syntactically well-formed, but cannot verify the actual visual rendering (ellipsis truncation, tooltip-on-hover) in a browser — **Yasser needs to visually confirm this in-browser** before it's considered fully verified. |
| 1.23 | 2026-08-27 | KCSC | Fixed the real root cause of the column-alignment inconsistency Yasser found in `RFQ Comparison Sheet Item`'s desk grid: not a Frappe bug, and not a currency-formatting issue (refuted — all 3 test quotations genuinely share the same currency/company) — `Zuckerman Security Ltd.` (23 characters) simply exceeds the 150px Supplier column width. Widened to 220px, sized against the real longest supplier name on this site with headroom, not hardcoded for today's 3 test suppliers alone. Checked the Item column defensively too: its actual displayed field (`item_code`, a Link showing the item's code, e.g. `SKU010`) is uniformly 6 characters across every real Item — left unchanged, since 140px already has large real-data headroom. Flagged, not silently resolved either way: genuine ellipsis+tooltip robustness (mentioned in this task's own framing) would need a dedicated Client Script, since Frappe's grid CSS has the class available but doesn't apply it automatically — not built here, since it wasn't part of the concrete steps given; left as an open, deliberate decision for later. Migrate confirmed clean afterward. Full detail in Known Issues. |
| 1.22 | 2026-08-27 | KCSC | Fixed `RFQ Comparison Sheet Item`'s desk grid, found by Yasser opening the doctype directly through the desk: `in_list_view` was never set on any field at Stage C's creation, so the grid showed nothing but row numbers. Set `in_list_view=1` with sensible column widths on all 8 meaningful fields, confirmed via a fresh `get_meta()` read, not just an error-free `save()`. Marked the parent doctype's generated/copied fields (`price_weight`, `lead_time_weight`, `generated_by`, `generated_on`, `request_for_quotation`) read-only, since this is a generated record, not meant for manual desk editing — `naming_series`/`items` deliberately left as-is. Checked the parent's field order too: `items` already sits last after the metadata fields, already sensible, nothing to reorder. Re-confirmed the existing real record (`RFQCS-2026-00001`) still loads with all 3 correct rows after both changes. Full detail in Known Issues. |
| 1.21 | 2026-08-27 | KCSC | RFQ Comparison Sheet, Stage C — the actual doctype and scoring engine. Created `RFQ Comparison Sheet` (parent, naming series `RFQCS-.YYYY.-`) and `RFQ Comparison Sheet Item` (child) as `custom=1` doctypes via the Document API; hit and fixed a real creation-order dependency (the parent's `Table` field validates its `options` against an *existing* DocType at insert time, so the child had to be created and committed before the parent could reference it — the first attempt's parent creation failed with `WrongOptionsDoctypeLinkError`, independently confirmed via fresh SQL before retrying, not assumed fixed). Added `proc_app/api/rfq.py`'s `generate_comparison_sheet(rfq_name)`, implementing the formula confirmed 2026-08-24 (Known Issues, entry below) exactly, including the missing-lead-time edge case: a supplier with no (or zero) `lead_time_days` scores 0 on that factor and is excluded from the `min_lead_time` reference calculation, so it can never distort what "fastest" means for the other real suppliers. Building real contrasting test data surfaced 2 genuine domain constraints requiring careful recovery, not code bugs — full detail in the Known Issues entry directly above. Verified the actual math by hand against the confirmed formula, not just that the code ran without error: 3 real suppliers (`MA Inc.` 100/3-days, `Zuckerman Security Ltd.` 80/14-days, `Summit Traders Ltd.` 90/no-lead-time) quoting the same item under a 60/40 price/lead-time weighting produced `price_score`/`lead_time_score`/`weighted_mark` values matching the formula exactly (e.g. Zuckerman's `lead_time_score = 3÷14×100 = 21.43`), correctly ranked 1st/2nd/3rd by `weighted_mark`, with Summit Traders' `lead_time_score` correctly `0` — and critically, confirmed MA Inc.'s own `lead_time_score` came out `100` (not diluted by Summit's placeholder `0`), proving the exclusion rule actually took effect rather than just checking Summit's own score in isolation. `RFQCS-2026-00001` and all 3 test `Supplier Quotation` documents deliberately left in place (not cleaned up) for Stage D's UI testing, confirmed to survive a clean `migrate` afterward. Also corrected a wrong assumption in this task's own framing: custom doctypes *do* need explicit `DocType` fixture export (confirmed via the existing `Purchase Order Amendment` precedent), not something to skip. |
| 1.20 | 2026-08-27 | KCSC | RFQ Comparison Sheet, Stage B: added `price_weight`/`lead_time_weight` (Percent) custom fields to `Request for Quotation`, and extended `proc_portal/api/rfqs.py`'s `create_rfq()` to accept, default (60/40, matching Yasser's original example — a sensible default, not a hardcoded requirement), and validate them server-side (`abs((price_weight + lead_time_weight) - 100) > 0.01` → `frappe.throw`), never trusting the client's own check alone. Verified both directions live via real HTTP as `procurement.officer.test`: a valid 70/30 combination succeeded (`PUR-RFQ-2026-00006`, weights independently confirmed persisted via fresh SQL), and an invalid 70/50 combination was genuinely rejected server-side with our own clear message (`"Price Weight and Lead Time Weight must sum to 100 (currently 120.0)."`), not a raw ERPNext error or a silent accept. Test RFQ cleaned up, independently reconfirmed gone. Note: `proc_portal`'s `rfqs.py`/`new-rfq.html`/RFQ list-and-detail pages already existed on this site going into this task, with no record of having been built in this project's own session history — read their real, current content directly (per standing practice) before extending, rather than assuming their shape. |
| 1.19 | 2026-08-27 | KCSC | RFQ Comparison Sheet, Stage A: `proc_app/api/rfq.py`'s `submit_quotation()` now captures `lead_time_days` and `expected_delivery_date` per item — both already existed natively on `Supplier Quotation Item` (confirmed via a prior read-only audit) but were never populated by the supplier submission flow. Purely additive: both fields optional, `supplier_portal`'s thin wrapper needed no change (confirmed genuine pass-through of the full `items` payload). Verified live via real HTTP as `supplier@kcsc.com.jo` against `PUR-RFQ-2026-00003`: submitted with real values (`lead_time_days=7`, `expected_delivery_date=2026-09-01`) — both persisted correctly, confirmed via direct SQL. Also verified backward compatibility explicitly: a second submission omitting both fields still succeeded (`lead_time_days` defaults to `0`, an Int field's natural null-coercion; `expected_delivery_date` stays `NULL`), confirming nothing existing was broken. Both test `Supplier Quotation` documents cleaned up; `quote_status` reset to `Pending`. **Flagged, not fabricated:** the task starting this work referenced a "confirmed weighted-scoring formula for future stages" — searched all 3 spec documents, found no prior record of one; recorded as an open item in Known Issues rather than inventing a formula to fill the gap. |
| 1.18 | 2026-08-23 | KCSC | Built `Physical Count Variance` as a custom Query Report, closing the last confirmed Stock Reports gap — joining `Stock Reconciliation`/`Stock Reconciliation Item` (fields confirmed exactly via meta first, not memory), filtered to genuine variances (`quantity_difference != 0`) on submitted documents only. Extended `setup_report_permissions()` with both `read` and `report` on `Stock Reconciliation` for Department Officer — unlike the two earlier report grants, this doctype had *no* existing permission for that role at all (not even the native `Stock User` role has read here), confirmed via a fresh DocPerm query before assuming `report` alone would suffice. **Found and fixed a second, distinct permission gate**: even after that grant, the report still blocked the test user — traced to the `Report` document's own `Has Role` table, a separate, one-time-snapshot mechanism unrelated to the `report` ptype check (full details in Known Issues); fixed by explicitly adding the missing roles to the report's own `roles` child table. **Found and recovered from a real broken document during live testing**: the first test attempt hit the same ambient-company-default bug already fixed twice elsewhere in this project, but console continuation masked the actual `submit()` failure behind a misleading success-looking print line; a fresh, independent SQL check caught it. Confirmed no real stock data was corrupted (`Bin.actual_qty` unaffected), corrected the broken document's `company` field directly to unblock a legitimate `cancel()` (which had also failed once for the same reason), then cleanly force-deleted it. Redid the test properly with `company` set explicitly from the start — succeeded immediately, verified the correct variance row via the actual report run as `operations.officer.test`, then cleaned up normally. Confirmed the now-permanent pre-commit hook handled this cycle's `export-fixtures`-induced workflow `is_active` drift automatically, with no manual fixture correction needed this time. |
| 1.17 | 2026-08-23 | KCSC | Closed the recurring workflow-fixture `is_active` drift for good, after it bit this project twice in one session with the same root cause (any `export-fixtures --app proc_app` run re-captures the workflows' live-active state). Added a tracked git pre-commit hook, `.githooks/pre-commit`, that automatically resets any `is_active: 1` found in `fixtures/workflow.json` back to `0` and re-stages it before every commit completes — formatted to exactly match `bench export-fixtures`'s own output (confirmed via a byte-for-byte round-trip check first, so the hook itself never introduces a spurious formatting diff). Activated for this clone via `git config core.hooksPath .githooks`. Verified live, not just written and trusted: deliberately drifted all 3 workflows to `is_active: 1` in the fixture file, staged and committed the drifted version — the hook fired, corrected it, and the actual committed blob (`git show HEAD:...`, not the working tree) confirmed all 3 back to `0` with formatting intact. **Real limitation, documented rather than glossed over:** `core.hooksPath` is a per-clone git config, not something a fresh `git clone` picks up automatically — anyone working from a new clone must run the one-time activation command themselves before this protection applies. |
| 1.16 | 2026-08-23 | KCSC | Audited the remaining 4 Stock Reports against ERPNext's native library (48 reports checked in the Stock module): `Stock Balance` (already OOB-verified) directly covers Stock Valuation, no new work needed. Confirmed genuine gaps for the other 3: `Itemwise Recommended Reorder Level` calculates a suggested reorder level from consumption history rather than comparing current stock against the item's actual configured level; the native Reorder Tool itself (`erpnext/stock/reorder_item.py`) is a silent scheduled background job with no viewable report/page at all; `Total Stock Summary` is a current-snapshot-only report with no period/movement breakdown; `Stock Ledger Variance` is a data-integrity check on ledger consistency, unrelated to physical stock counts; no report references Stock Reconciliation at all despite its documents genuinely holding correct per-document variance data. Built `Items Below Reorder Level` as a custom Query Report, joining `Item Reorder`/`Item`/`Bin`, matching ERPNext's own real reorder criteria (`projected_qty <= reorder_level`, confirmed from `reorder_item.py`'s actual logic) exactly, so it shows what the background job would act on. Extended the existing `setup_report_permissions()` (not duplicated) to also grant the `report` ptype on `Item` for Department Officer and Procurement Officer. Configured real, permanent test data (`SKU010` at `Stores - K`, reorder level `10` vs. actual stock `0`) rather than a throwaway fixture — verified live as `operations.officer.test`: report runs successfully, 7 correct columns, exactly 1 row, matching the configured test data precisely. **Found the same workflow-fixture drift a second time, now understood structurally rather than as a one-off**: re-running `export-fixtures` to capture this new report also re-exported all 3 workflows' current live `is_active=1` into the fixture, since `Workflow` shares the same fixtures list — traced to `export-fixtures` capturing live DB state, not `bench migrate`'s `sync_fixtures()` (which actually imports, the opposite direction). Fixed the fixture file again (live DB left active); documented as a standing, recurring gotcha in Known Issues, not a permanently-fixed bug — future sessions must re-check this after every `export-fixtures` run for this app. Stock Movement Summary and Physical Count Variance remain open gaps, not yet built. |
| 1.15 | 2026-08-23 | KCSC | Development Phase 5 (Reports) started. Full audit of the 7 Procurement Reports against ERPNext's native reporting library (119 reports checked in Buying/Accounts/Stock): `Supplier Quotation Comparison` and `Invoice Aging` (via `Accounts Payable`'s real `ageing_based_on`/`range` filters, confirmed genuine, not just a name match) are cleanly OOB-reusable; `Purchase Order Analysis`/`Purchase Order Trends` cover PO Status Tracker well (delivery + billing status) but lack a dedicated payment-status column; `Purchase Register`/`Item-wise Purchase Register` can produce Spending by Supplier/Category via the report viewer's own Group By, but aren't purpose-built dedicated reports; `Procurement Tracker` is the closest match for Purchase Requisition Status but tracks the linked PO's native `status`, not our custom `workflow_state` — confirming this as the one genuine gap. Built `Purchase Requisition Status` as a custom Query Report (Requisition, Date, Requester, Requesting/Concerned Department, Approval Stage, Type, Total Qty, Estimated Value), joining `Material Request`/`Material Request Item`. Checked `rate`/`amount` data-honesty first (see Known Issues) before treating "Estimated Value" as reliable. Found and fixed a real permission gap along the way: Query Reports need a distinct `report` ptype separate from read/write/create/submit, confirmed via `frappe/desk/query_report.py` source — added `setup_report_permissions()` granting it to Procurement Officer. Verified live as `procurement.officer.test`: report runs successfully, 9 correct columns, 8 real rows, sample data confirmed accurate against the underlying documents. |
| 1.14 | 2026-08-23 | KCSC | RFQ Stage C follow-up: fixed 3 real bugs found by the Stage C integration test in `proc_app/api/rfq.py`'s `submit_quotation()` — a supplier-facing endpoint "done" since Phase 2 that had never once been exercised with real data. (1) Auto-fill `warehouse` from `Item Default`'s per-company default, since a supplier has no way to provide the buyer's internal warehouse; clear actionable error if none exists. (2) Set the new `Supplier Quotation`'s `company` explicitly (derived from the RFQ), fixing a wrong-company failure that surfaced once (1) was fixed — the same class of ambient-default bug already fixed once before in `proc_portal`'s `create_request()`. (3) Added `sq.submit()` after insert — confirmed with the user as a deliberate design decision, not assumed — since ERPNext's own `quote_status` sync (`Request for Quotation Supplier` → "Received") only runs from `Supplier Quotation.on_submit`, and the function previously left the document as an unsubmitted Draft forever. Verified all 3 fixes end-to-end via real HTTP + fresh SQL: `PUR-SQTN-2026-00004` created, submitted (docstatus 1), correct company (`KCSC`), correct warehouse (`Stores - K`), `quote_status` correctly flipped to `Received` once the test payload also included the RFQ item's own child-row `name` (a 4th finding, clarified as a test-methodology gap, not a code bug — the real frontend already threads this through naturally). Error path also verified: an item with no `Item Default` produces the clear custom message, not ERPNext's raw one. Full details in Section 4's Known Issues entry. Test artifacts cleaned up; `quote_status` reset to `Pending` for a clean Stage B slate. |
| 1.13 | 2026-08-23 | KCSC | RFQ Stage A: built the `RFQ Approval` workflow on `Request for Quotation` (Draft → Pending Approval → Approved/Rejected, Procurement Officer throughout, self-approval by design — same pattern as `Purchase Order Amendment Approval`), reusing that build's existing Workflow States (`Draft`, `Pending Approval`) and Workflow Action Masters (`Submit`) — `Approved`/`Rejected` states and `Approve`/`Reject` actions confirmed already seeded by Frappe core, no new fixture entries needed for those. New `setup_rfq_permissions()` in `setup.py`, matching the file's established delete-then-recreate idempotent style for the main grant (full read/write/create/submit on `Request for Quotation` for Procurement Officer) and the existing "if not exists" style for the secondary grant (read-only on `Supplier`, needed to populate the RFQ form's suppliers child table) — deviated from the task's proposed "if not exists" pattern for the *main* grant specifically, since that would have been inconsistent with every other main-doctype permission function in this file. Fixture coverage required 2 `hooks.py` filter additions, caught the same way as the PO Amendment build: `RFQ Approval` added to the `Workflow` filter (the first `export-fixtures` run silently captured nothing for it, exactly the same gotcha as before) and `Request for Quotation` added to the `Custom Field` filter (for its auto-created `workflow_state` field). Verified end-to-end via real workflow cycle as `procurement.officer.test`: created `PUR-RFQ-2026-00003` (2 real ERPNext validation gaps found and fixed in the *test script* along the way — see Known Issues), `Submit` → `Pending Approval`, `Approve` → `Approved`/docstatus 1, independently re-verified via fresh SQL. **Deliberately not cleaned up** — kept as real test data for Stage C's `supplier_portal` integration test. All 3 workflows (`Material Request Approval`, `Purchase Order Amendment Approval`, `RFQ Approval`) confirmed active after a clean `migrate` (exit 0, no orphans); only `RFQ Approval` needed reactivation, since it had never been activated before this task. |
| 1.12 | 2026-08-23 | KCSC | Documented a decision, not a fix: found via live permission testing in `proc_portal` that a user holding both Department Officer and ERPNext's native Stock User role can create Material Requests, since Stock User natively has `create=1` via a pre-existing OOB core `DocPerm` grant — Frappe's permission model being additive across roles, this genuinely allows creation despite Department Officer's own design being review/approve only. Confirmed with Yasser as acceptable real-world behavior (warehouse/stock staff plausibly need to raise their own requests too), not tightened. Recorded in Section 4's Known Issues & Decisions Log so it isn't mistaken for an oversight later. |
| 1.11 | 2026-08-22 | KCSC | Documented a real limitation Yasser found via hands-on testing: reaching "Approved" on a Purchase Order Amendment has no automated effect — it does not update the linked Purchase Order's fields and does not notify the supplier. Framed explicitly as a deliberate scope decision, not a bug: added a note to the Phase 4 row in Section 2 (Development Phases) and a Known Issues entry, both flagging that automation is deferred until real business rules are confirmed (post-workshop), with future work noted (auto-update PO fields, supplier notification via the still-unbuilt Notification Master List). |
| 1.10 | 2026-08-22 | KCSC | Fixed "Insufficient permission for Purchase Order", found via desk testing of the new Purchase Order Amendment form — Procurement Officer could not select a Purchase Order on the form's link field, a gap specific to this feature and never needed by Material Request approval. Confirmed no existing `Custom DocPerm` covered it beforehand. Extended the existing `setup_po_amendment_permissions()` in `setup.py` with an idempotent check-before-create grant of `read=1` on `Purchase Order` for `Procurement Officer`, alongside that function's existing PO Amendment doctype permissions. Verified via `Custom DocPerm` query and a clean `migrate`. Both custom workflows (Material Request Approval, Purchase Order Amendment Approval) reverted to `is_active: 0` on this migrate as expected per the standing documented quirk, and were reactivated via `doc.save()`, independently re-verified via fresh SQL. |
| 1.9 | 2026-08-14 | KCSC | Fixed "Insufficient permission for Price List", found via hands-on desk testing of the New Material Request form — a gap portal/API testing structurally couldn't have caught, since it never renders that field. Added `"Price List"` to `setup.py`'s existing `SUPPORTING_DOCTYPES` list, extending the same read-only 4-role pattern already covering Item/Item Group/UOM/Warehouse/Brand/Company/Department, rather than a one-off fix. Verified all 4 `SUPPORTING_ROLES` gained `read=1` and the pre-existing native roles' core permissions (`Purchase User`, `Sales User`, etc.) were unaffected, and confirmed the change survives a clean `migrate`. |
| 1.8 | 2026-08-14 | KCSC | Fixed a real permission gap found via hands-on desk testing (Yasser): `operations.officer.test` couldn't create a Stock Entry from an "Approved - Issue" Material Request, hitting "No permission for Stock Settings". Diagnosed first — confirmed `Stock User` role was still present (not lost) and that core ERPNext's `Stock Settings` DocPerm genuinely never granted `Stock User` read access (only `Sales User`/`Stock Manager`), a pre-existing OOB gap unrelated to anything in this app. Fixed narrowly via a new `setup_stock_settings_permission()` in `setup.py` (same pattern as every other permission function here), granting `read=1` on `Stock Settings` to `Department Officer` only — not the native `Stock User` role, to avoid an unaudited system-wide change. Verified via `Custom DocPerm` query and a clean `migrate` (`after_migrate` re-runs the new function idempotently). Noted, not a problem: adding the first `Custom DocPerm` for a doctype makes Frappe use it exclusively going forward, so the pre-existing `Sales User`/`Stock Manager` core permissions were automatically mirrored into `Custom DocPerm` too — confirmed their access is unchanged. |
| 1.7 | 2026-08-14 | KCSC | Built and tested Development Phase 4 — Purchase Order Amendment, the one confirmed custom-doctype gap from the original OOB-vs-Custom audit. New `Purchase Order Amendment` DocType (`custom=1`, `is_submittable=1`, naming series `POA-.YYYY.-`; fields: `purchase_order`, `amendment_type`, `description`, `reason`, `requested_by`, `workflow_state`) plus a `Purchase Order Amendment Approval` Workflow (Draft → Pending Approval → Approved/Rejected, Procurement Officer throughout), same proven pattern as the Material Request Approval Workflow. A separate internal record — does not replace supplier_portal's existing Comment-based amendment request. First attempt to create the DocType failed with `ValidationError: Cannot set Assign Submit if not Submittable` (missing `is_submittable: 1` alongside a `submit: 1` permission) — confirmed nothing was persisted before retrying with the fix (see Known Issues). `Approve`/`Reject` Workflow Action Masters and the `Approved`/`Rejected` Workflow States turned out to already be seeded by Frappe core itself at site-creation time (`frappe/utils/install.py`), confirmed by reading the source rather than assumed — so only the new `Pending Approval` state needed adding, and no Workflow Action Master fixture changes at all. `Purchase Order Amendment` permission for Procurement Officer added as a new `setup_po_amendment_permissions()` function in `setup.py`, matching the file's existing idempotent delete-then-recreate style, registered in `after_migrate()`. Fixture coverage required 3 `hooks.py` filter updates (new `DocType` entry for the custom doctype itself, since `custom=1` doctypes have no `.json`/`.py` files; `Pending Approval` added to the `Workflow State` filter; `Purchase Order Amendment Approval` added to the `Workflow` filter) — the first `export-fixtures` run silently captured none of the new records until these were added, caught by checking the actual filter list Frappe printed rather than assuming coverage. Verified end-to-end with real HTTP-free but real DB test: created `POA-2026-00001` against a real PO, `Submit` → `Pending Approval`, `Approve` → `Approved`/docstatus 1, all independently re-verified via fresh SQL queries (not just same-session reads). Cleaned up correctly per both of today's earlier console lessons: explicit `frappe.db.commit()` after every write, and `cancel()`'s success (docstatus==2) checked before the force-delete. `is_active` reset to `0` in the fixture afterward via `.save()` (not raw `db.set_value()`), per the standing cache-invalidation policy — confirmed both this workflow and Material Request Approval show `is_active: 0` in the re-exported fixture. |
| 1.6 | 2026-08-14 | KCSC | Added `Department.default_warehouse` (Link → Warehouse, optional) — same pattern as `department_manager`/`department_officer` added earlier. Fixture-exported via the existing dt-based Custom Field filter in `hooks.py` (already covered `Department`, no filter change needed). Set a real value on `Operations - K` (`Goods In Transit - K`, matching company) for use as a live test case by `proc_portal`'s New Request form. Consumed there as a convenience default — pre-filled but still editable, per `PROC_PORTAL_SPEC.md` Changelog v1.4. **Correction (2026-08-22):** `Operations - K`'s `default_warehouse`, originally set only to serve as a live test case during this build (not a considered business choice for real usage), was corrected to `Stores - K` per Yasser's review during hands-on testing — a more sensible default for a department expected to issue stock from general stores. |
| 1.5 | 2026-08-14 | KCSC | Stock Management (proc_app Development Phase 3) verified OOB-complete via live testing — Item, Warehouse, Stock Entry, Stock Reconciliation, and all 3 native stock reports confirmed genuinely working, no custom code needed, matching the earlier paper-based OOB-vs-Custom audit. One honest gap documented: Reorder Tool's actual trigger behavior untested due to no reorder-level data configured. Also recorded 2 reusable process lessons about bench console testing pitfalls (uncommitted writes vanishing on session exit; unchecked cancel() failures leading to force-deleting still-submitted documents), and flagged 2 pre-existing negative-stock data anomalies found but not investigated (out of scope). |
| 1.4 | 2026-08-14 | KCSC | Doctype Migration Stage 3 (final stage — closes the whole Doctype Migration phase): split supplier_portal's API/hooks business logic so reusable core domain logic lives here, thin auth/ownership wrappers stay in supplier_portal, with EXACT preservation of whitelisted method signatures (zero frontend changes needed). Proof of concept first: `acknowledge_po()`/`request_amendment()` (from `orders.py`) — found and fixed two real behavior-preservation risks in the originally sketched split before testing, not after: the 3 acknowledgment fields have `allow_on_submit=0`, so the core function had to keep the original's raw `frappe.db.set_value()` mechanism rather than switching to `doc.save()` (which Frappe would have rejected on a submitted PO); and `require_supplier_login()` (built for page controllers, raises `frappe.Redirect`) was dropped from the wrapper in favor of the original's inline `get_supplier_for_current_user()` check, which returns JSON as an API method must. Verified live: real acknowledgment succeeded, DB-persisted correctly, and the second-call "already acknowledged" domain-rule guard (a genuine new addition, not in the original) correctly blocked a repeat. Then the remaining 3 files: `submit_asn()` (asn.py), `get_po_items_for_invoice()`/`submit_invoice()`/`submit_dispute()` (invoices.py), `submit_quotation()` (rfq.py) — same pattern, `proc_app/api/{asn,invoices,rfq}.py` created. `get_po_items_for_invoice()` (read-only) was deliberately split too despite being a query helper, since it contains real reusable domain logic (GRN-quantity aggregation), not a pure pass-through — the ownership check and `[]`-on-error convention stayed in the wrapper. `submit_quotation()`'s core function needed an explicit `supplier` parameter (unlike the others) since a Request for Quotation lists multiple suppliers in a child table, unlike Purchase Order/Purchase Invoice which each belong to exactly one — an internal core-function detail only, the whitelisted signature is unchanged. `hooks_handlers.py` (already disabled, `doc_events = {}`, since the duplicate-notification fix) relocated here as dead code being organized correctly, not a live behavior change. **A real, pre-existing bug was found and fixed along the way, not introduced by the split:** live-testing `submit_invoice()` end-to-end with real HTTP + real data (`PUR-ORD-2026-00007`) — apparently for the first time ever, closing the exact gap Phase 1 had documented ("invoice write flows remain untested pending a proper UAT dataset") — hit `frappe.PermissionError` on the `Account` doctype, then after fixing that, a second wall on `Item`. Both are supplier_portal's own missing `Supplier Portal User` DocPerm entries (its `setup.py` `DOCTYPE_PERMS` list never included them), fixed by adding `("Account", 1, 0, 0, 0)` and `("Item", 1, 0, 0, 0)` to that list and re-running `setup_portal_permissions()` — which is only wired to `after_install`, not `after_migrate`, so `bench migrate` alone would not have applied it; had to invoke the function directly. **A second, independent problem surfaced while doing that**: `setup_portal_permissions()` itself contained `frappe.db.set_value("Role", "Supplier Portal User", "home_page", "supplier-portal")` — the exact mechanism already found and fixed as the Administrator-redirect bug in an earlier session (replaced with an `on_login` hook keyed on `User.default_app`). Re-running this function would have silently reintroduced that bug. Removed the line entirely (replaced with an explanatory comment) rather than just working around it once, so a future fresh install or re-run of this function can't reintroduce it either. Verified after each permission grant that `Role.home_page` stayed `NULL`. Final retest succeeded genuinely: `ACC-PINV-2026-00007` created and DB-verified, then cleaned up. |
| 1.3 | 2026-08-14 | KCSC | Doctype Migration Stage 2: received fixture ownership of 7 custom fields (3 on Purchase Order, 4 on Supplier) from supplier_portal. Confirmed via database audit that these were the ONLY custom fields on either doctype — no risk of accidentally sweeping in unrelated fields when scoping the fixture filter. Nothing moved at the database level, only fixture-export ownership. Verified via `export-fixtures` round-trip on both apps and a clean migrate. |
| 1.2 | 2026-08-14 | KCSC | Doctype Migration Stage 1: moved Supplier ASN, Supplier ASN Item, and Supplier Invoice Dispute doctypes from supplier_portal to proc_app (created Module Def 'Proc App' first, since proc_app had no doctypes of its own until now — everything it owned before this was fixture-only: Custom Field, Role, Workflow). Confirmed zero real data existed in any of them before the move — a pure structural migration, no data risk. **Two real problems were hit and fixed during this migration, not a single clean pass — documented honestly rather than smoothed over:** (1) The first `bench migrate` after the move silently deleted all 3 `tabDocType` records as "orphans" (exit code 0, no literal "Error"/"Traceback" text — this was caught by explicitly checking for the "orphan" cleanup step in the log, not by grepping error keywords alone). Root cause: the new Module Def "Proc App" was created directly in the database but `apps/proc_app/proc_app/modules.txt` was never updated to list it, so migrate's doctype-discovery scan never recognized the moved files' location as valid, and its orphan-cleanup pass deleted the now-unreachable `tabDocType` records (the underlying DB tables themselves were never dropped — confirmed via `SHOW TABLES`, and since all 3 doctypes had 0 rows to begin with, no actual data was lost). Fixed by adding `Proc App` to `modules.txt`. (2) A second migrate still didn't re-register the doctypes — `proc_app` didn't even appear in the "Updating DocTypes for X" sync list. Root cause, confirmed via `frappe.scrub()`/`get_module_path()` rather than assumed: the module "Proc App" scrubs to `proc_app`, and Frappe expects that module's doctypes at `apps/proc_app/proc_app/proc_app/doctype/...` — a third `proc_app` nesting level, matching the same pattern `supplier_portal/supplier_portal/supplier_portal/` already used — but the files had been placed one level shallower, at `apps/proc_app/proc_app/doctype/...`. Fixed by moving the 3 folders one level deeper to match `get_module_path()`'s actual expected location. A third migrate then correctly synced `proc_app` for the first time, registered all 3 `tabDocType` records under module "Proc App", with zero orphans. Verified via functional smoke tests: created and deleted a real `Supplier ASN` (against a real submitted PO) and a real `Supplier Invoice Dispute` (against a real submitted Purchase Invoice, with a corrected `dispute_type` select value after an initial wrong-test-data failure) — both succeeded cleanly post-move. Confirmed supplier_portal's existing API code (`asn.py`, `invoices.py`) references these doctypes by name only (`"doctype": "Supplier ASN"` style), never by direct Python import path — zero code changes needed there, since Frappe resolves doctypes by name/module in the database regardless of which app's folder currently contains them. Custom field migration (7 fields: 3 on Purchase Order, 4 on Supplier) and the API/hooks business-logic split (`acknowledge_po`, `submit_asn`, `submit_invoice`, `submit_dispute`, `request_amendment`, plus `hooks_handlers.py`) are deliberately deferred as a separate follow-up task, not bundled into this stage. |
| 1.1 | 2026-08-09 | KCSC | Documented a real permission gap found by Yasser during hands-on browser testing: Department Officer could not create a Stock Entry from the Approved - Issue state, since our permission matrix never granted anything on the Stock Entry doctype itself. Fixed by assigning ERPNext's native 'Stock User' role directly to the test user (a manual, per-deployment user-assignment action, not a proc_app fixture change) — confirmed working. Documented in Section 3.6 as an operational requirement: any real Department Officer needs Stock User (or equivalent) in addition to the Department Officer role. Also clarified/confirmed: a document collision occurred earlier in testing when console-based and browser-based testing ran against the same live site simultaneously, causing a browser-created document to be swept up by a console cleanup script targeting the same auto-generated name — noted as a process lesson (don't run parallel console/browser testing on shared live data), not a product bug. |
| 1.0 | 2026-08-09 | KCSC | MILESTONE: redesigned the Forward-to-Purchase branch after discovering (via manual browser testing) that ERPNext's native Create buttons are gated by material_request_type, not workflow_state — the original one-document design had no working native path to create a Stock Entry from a Purchase-type request. Solution: requests now start as material_request_type='Material Issue'; Forward-to-Purchase now spawns a new, separate, properly-typed Purchase request (linked via new source_material_request field) instead of continuing in the same document, avoiding any need for custom UI buttons. Original document closes via a new 'Forwarded to Purchase' terminal state plus ERPNext's native update_status('Stopped') call (confirmed correct via code audit, not a raw field write). New doc_events hook (material_request_hooks.py) handles the spawn, with idempotency guards against re-triggering and duplicate spawning. Both branches (Issue-from-Stock, Forward-to-Purchase) tested end-to-end with zero exceptions. Test documents cleaned up. Workflow structure now: 9 states (was 8), 10 transitions (one modified). This is the first version considered feature-complete for the reference example's full scope. |
| 0.9 | 2026-08-08 | KCSC | MILESTONE: Full live test of the Material Request Approval Workflow's purchase branch, end-to-end, using real apply_workflow() calls under 5 distinct test user identities (not Administrator, not manual field manipulation). All stages passed: Submit, Requesting-Dept Approval (wrong-department block proven), Concerned-Dept Review (wrong-role block proven), Forward to Purchase, Concerned-Dept Manager Approval, Procurement Approval, ending in Approved - Purchase with docstatus correctly transitioning to Submitted (1). Both independent gating mechanisms (role-based allowed, department-matching condition) confirmed working correctly and independently — neither substitutes for the other. Issue-from-Stock branch and Reject paths not yet tested (see Section 3.7) — expected to work via the same proven mechanisms but not explicitly verified. Test document cleaned up (cancelled + deleted) after successful verification. Workflow left active at the DB level for continued work; will revert to inactive on next migrate per the documented fixture behavior (Section 3.6). |
| 0.8 | 2026-08-08 | KCSC | Found and fixed a second, distinct activation bug: after re-activating the Workflow via frappe.db.set_value() (bypassing the Document API), Frappe's workflow-lookup cache remained stale (cached empty string from an earlier inactive-state lookup), because get_workflow_name()'s cache check only re-queries on None, not on falsy values — so is_active=1 in the DB had no effect until the specific cache key was cleared. Traced by checking is_active directly (confirmed 1, ruling out the previously-fixed fixture-resync cause), then inspecting the cache directly rather than guessing. Fixed by clearing the specific stale cache key (frappe.cache.hdel). Updated Section 3.6's activation-procedure guidance to specify using the Document API (doc.save()) for activation going forward, not raw db.set_value(), to avoid this recurring. Cleaned up a second invalid test document. Ready to retry test creation. |
| 0.7 | 2026-08-08 | KCSC | Diagnosed and resolved a subtle bug during live testing: bench migrate silently reverted the Workflow's is_active flag back to 0 (its committed fixture value from before activation), because Workflow structure is fixture-managed but is_active isn't meant to persist as 1 — traced through cache inspection and direct DB queries before finding the actual root cause (fixture re-sync on migrate), ruling out caching as a false lead. Decision: is_active stays permanently committed as 0 in the fixture (correct default for fresh installs); activation is now documented as a deliberate, non-persisted runtime action (Section 3.6). Cleaned up one invalid test document (MAT-MR-2026-00003, created while workflow was unexpectedly inactive, workflow_state NULL) via force delete. Re-activated for continued testing. |
| 0.6 | 2026-08-08 | KCSC | Set up live test data: 2 existing departments (Accounts - K, Operations - K) assigned department_manager/department_officer; 5 test users created (System User type) with correct roles. Activated the Material Request Approval Workflow (is_active: 1) on a confirmed-empty Material Request table — no pre-existing documents affected. First test creation attempt failed with PermissionError: the 4 custom roles had zero DocPerm on Material Request — workflow transition gating and doctype-level permissions are separate Frappe mechanisms, and only the former had been built. Added proc_app/setup.py granting a permission matrix (Department User: create+read+write; Department Manager: read+write; Department Officer/Procurement Officer: read+write+submit), registered via after_migrate (deliberate deviation from supplier_portal's after_install pattern — documented in Section 3.6). Verified idempotent across a migrate re-run. Ready to retry test document creation. |
| 0.5 | 2026-08-08 | KCSC | Completed Part C: added department-matching conditions to 7 of 10 Workflow transitions on 'Material Request Approval', scoping Department Manager/Officer approvals to the specific department linked on each request (via frappe.session.user checks against Department.department_manager / department_officer). Verified via fresh DB re-fetch (not just in-memory state) that exactly the intended 7 transitions carry conditions and 3 correctly remain unconditioned (Submit, and both Procurement Approval actions, which are role-only gates by design). Documented a known limitation in Section 3.6: the initial Submit step doesn't verify the submitter actually belongs to the requesting_department they're filing on behalf of — deferred, no Employee-to-Department concept exists yet. Workflow remains inactive (is_active: 0) and untested against real documents — next step is a live test before considering activation. |
| 0.4 | 2026-08-08 | KCSC | Built the Material Request Approval Workflow (inactive, is_active: 0): 4 custom fields (Material Request: requesting_department, concerned_department; Department: department_manager, department_officer), 4 new roles (Department Manager, Department Officer, Department User, Procurement Officer), 8 Workflow States, 3 new Workflow Action Masters, and the Workflow document itself (8 states, 10 transitions, role-based gating, no department-matching conditions yet). All captured as proc_app fixtures via export-fixtures, verified via migrate. Frappe's auto-created workflow_state custom field on Material Request confirmed and fixture-captured. Self-approval left at Frappe's default (allowed) — confirmed as a deliberate decision, documented in Section 3.6. Workflow remains inactive and role-based conditions are not yet department-scoped (Part C, not yet started) — do not activate or rely on this workflow enforcing department-specific routing yet. |
| 0.3 | 2026-08-07 | KCSC | Documented Material Request Approval Workflow design (new Section 3) — custom fields, roles, workflow states/transitions, based on a real example workflow. Confirmed via live ERPNext audit: Material Request has no existing department field, Department has no manager field (both need custom fields), Workflow Transition supports condition-based routing (confirmed viable), native Create actions (Stock Entry, Purchase Order, RFQ) already exist on Material Request. Free-text/not-yet-in-system item support deliberately deferred — confirmed to require genuine custom code (item_code is framework-level mandatory), not just configuration. Design not yet built — this is documentation only. |
| 0.2 | 2026-08-06 | KCSC | Received the relocated cross-app architecture content (App Boundary & Ownership, OOB-vs-Custom Decision Log, Notifications Master List) from SUPPLIER_PORTAL_SPEC.md Sections 16-18, now living in this repo as PROC_SUITE_ARCHITECTURE.md. Updated Section 1.2 and Section 3 accordingly. |
| 0.1 | 2026-08-03 | KCSC | App scaffolded as `karama_proc`, then renamed to `proc_app` (App Title: KCSC Proc) before any real content was added. Spec file created/renamed to PROC_APP_SPEC.md accordingly. No functional content yet. |

---

*This document is the single source of truth for the KCSC Proc project. All team members and AI coding sessions must keep it current.*
